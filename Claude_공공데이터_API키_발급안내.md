# 공공데이터포털 오픈API 인증키 받는 방법

## 무엇을 받는 것인가

공공데이터포털(`data.go.kr`)은 정부·공공기관이 가진 자료를 프로그램이 가져다 쓸 수 있게 열어둔 곳입니다.
그 자료를 가져오려면 **인증키**가 필요합니다. 「누가 요청했는지」를 알리는 열쇠입니다.

인증키는 **무료**이고, 회원가입만 하면 받을 수 있습니다.

---

## 어떤 자료가 있나

「공공데이터」라고 하면 막연하지만, 실제로는 **업무에 바로 쓰는 자료들**이 들어 있습니다.

**부동산**

- **국토교통부 실거래가** — 아파트·오피스텔·연립다세대·단독다가구·상업업무용·토지·공장,
  **매매와 전월세 전부.** 시세 사이트의 추정가가 아니라 **실제 신고된 거래 금액**입니다
- **건축물대장** — 표제부, 층별 개요, 전유부, 주택가격
- 아파트 단지 기본정보, 토지이용계획

**공매·경매**

- **온비드 공매** — 물건 목록, 공고 내용, **입찰 결과와 낙찰가**, 유찰 회차, 조회수 상위 물건
- 캠코 국유재산·체납 압류재산

**거래처·기업**

- **국세청 사업자등록 상태** — 이 사업자가 지금 정상인지, 휴업·폐업인지
- **국민연금 사업장 가입현황** — 실제 직원 수 규모
- 금융위 기업 재무정보, 나라장터 조달 입찰

**생활·행정**

- 우체국 주소검색, 법정동코드
- 소상공인 상가업소 정보 (상권 분석)
- 도시철도 노선

이것은 일부입니다. 포털에서 검색해 보면 부처·지자체·공사가 올린 자료가 수만 건 있습니다.

> **왜 이것이 중요한가**
> 블로그나 요약 사이트의 숫자는 누군가 가공한 값입니다.
> 여기 있는 것은 **기관이 직접 올린 원본**입니다. 출처를 대라고 하면 댈 수 있는 자료입니다.

---

## 순서

### 1. 회원가입

```
https://www.data.go.kr
```

오른쪽 위 **회원가입** 을 누르고 일반 회원으로 가입합니다.

### 2. 쓸 API를 찾습니다

상단 **공공데이터** 메뉴에서 검색하거나, 검색창에 이름을 넣습니다.

검색 결과에는 두 가지가 섞여 나옵니다.

| 구분 | 무엇인가 |
|---|---|
| **파일데이터** | 엑셀·CSV 파일을 내려받는 것 |
| **오픈 API** | 프로그램이 직접 불러 쓰는 것 ← **이것을 찾아야 합니다** |

목록 왼쪽 필터에서 **오픈 API** 만 켜두면 편합니다.

### 3. 활용신청 — 여기가 핵심입니다

**인증키를 받았다고 모든 자료를 쓸 수 있는 것이 아닙니다.**

| | |
|---|---|
| **인증키** | 계정당 **하나**. 한 번 받으면 끝 |
| **활용신청** | **API 하나하나마다 따로** 해야 함 |

실거래가를 쓰려면 실거래가 API에 활용신청을 하고,
공매 자료를 쓰려면 온비드 API에 따로 또 신청해야 합니다.
**신청하지 않은 API는 인증키가 있어도 응답하지 않습니다.**

쓸 자료가 다섯 개면 활용신청도 다섯 번입니다. 한 번에 몰아서 해두는 편이 낫습니다.

**신청 방법**

API 제목을 눌러 상세 페이지로 들어간 뒤 **활용신청** 버튼을 누릅니다.

`[검증필요]` 입력 화면의 항목은 대체로 아래와 같습니다. API마다 조금씩 다를 수 있습니다.

| 항목 | 어떻게 |
|---|---|
| **활용목적** | 「참고자료」 또는 「앱 개발」 중에 고르고 한 줄 적으면 됩니다 |
| **상세기능 선택** | 그 API가 제공하는 기능 목록입니다. **전부 체크**해 두십시오 |
| **라이선스 동의** | 체크 |

**상세기능을 빠뜨리지 마십시오.** 예를 들어 실거래가 API에 「아파트 매매」만 체크하고 「아파트 전월세」를 빼면, 전월세는 나중에 다시 신청해야 합니다. 체크는 공짜이므로 전부 켜두는 것이 맞습니다.

신청 버튼을 누르면 끝입니다. 대부분 즉시 승인됩니다.

**같은 기관 자료라도 API가 나뉘어 있습니다**

「국토교통부 실거래가」는 하나가 아닙니다. 실제 등록된 이름을 보면 이렇습니다.

```
국토교통부_아파트 매매 실거래가 자료
국토교통부_아파트 전월세 실거래가 자료
국토교통부_아파트 분양권전매 실거래가 자료
국토교통부_상업업무용 부동산 매매 실거래가 자료
국토교통부_토지 매매 실거래가 자료
국토교통부_공장 및 창고 등 부동산 매매 실거래가 자료
```

**유형마다 완전히 별개의 API**이고, 각각 활용신청을 해야 합니다.

처음에는 **실제로 쓸 것 두세 개만** 신청하고, 필요할 때 추가하십시오.

### 4. 승인 확인

`[검증필요]` 대부분의 API는 **자동승인**이라 신청 즉시 승인됩니다.
심의가 필요한 일부 API는 하루 이틀 걸릴 수 있습니다. 상세 페이지에 자동승인 여부가 표시됩니다.

승인 상태는 여기서 봅니다.

```
마이페이지 → Open API → 활용신청 현황
```

화면 위에 칸이 네 개 나옵니다.

| 칸 | 뜻 |
|---|---|
| **전체 API활용 신청 현황** | 지금까지 신청한 총 건수 |
| **활용 신청** | 아직 처리되지 않은 것 — `신청` · `보류` · `반려` |
| **활용 중** | **쓸 수 있는 것** — `승인` · `활용변경` · `활용연장` |
| **만료·중지** | `기간만료` · `활용중지` |

`활용 중` 의 **승인** 숫자에 잡히면 쓸 수 있는 상태입니다.
`반려` 에 잡혔다면 신청 내용을 고쳐 다시 신청해야 합니다.

그 아래 **오픈 API 활용신청 검색** 에서 `데이터명` 으로 찾을 수 있습니다.
신청 건이 수십 개로 늘어나면 이 검색 없이는 못 찾습니다. `처리상태` 로 걸러 보는 것도 됩니다.

### 5. 인증키 확인

```
마이페이지 → Open API → 인증키 발급현황
```

좌측 메뉴에서 `활용신청 현황` 바로 아래에 있습니다.
여기에 **일반 인증키** 가 있습니다.

**인증키는 계정당 하나입니다.** API를 40개 신청했든 하나만 신청했든 키는 같습니다.
API를 새로 신청할 때마다 키를 다시 받을 필요가 없습니다.

---

## Encoding 키와 Decoding 키

인증키가 두 줄로 보입니다. 같은 키인데 표기 방식이 다릅니다.

| 종류 | 생김새 | 언제 쓰나 |
|---|---|---|
| **Encoding** | `%2B`, `%3D` 같은 기호가 섞여 있음 | 주소창에 직접 붙여넣어 시험할 때 |
| **Decoding** | `+`, `=` 가 그대로 보임 | **프로그램·MCP 설정에 넣을 때** |

`[검증필요]` 프로그램에 넣을 때는 대개 **Decoding** 키가 맞습니다.
넣었는데 인증 오류가 나면 **다른 쪽 키로 바꿔** 보십시오. 이 둘을 바꿔 넣는 것이 가장 흔한 실수입니다.

---

## 트래픽 한도

`[검증필요]` **API마다 다릅니다.** 상세 페이지의 「활용신청 상세기능정보」에 하루 몇 건까지 쓸 수 있는지 적혀 있습니다.

계정은 두 종류입니다.

| 계정 | 용도 |
|---|---|
| **개발계정** | 시험·개인 활용. 활용신청하면 기본으로 받는 것 |
| **운영계정** | 실제 서비스 운영. 한도를 늘리려면 별도 신청 |

개인이 업무에 참고용으로 쓰는 수준이면 **개발계정으로 충분합니다.**
한도를 넘기면 그날은 응답이 오지 않고, 다음 날 다시 풀립니다.

---

## 활용기간은 2년입니다

`[확정]` 활용신청이 승인되면 **신청일로부터 2년**간 쓸 수 있습니다.
목록의 각 항목에 `신청일` 과 `만료예정일` 이 함께 표시됩니다.

```
계정  개발 | 신청일 2026-04-17 | 만료예정일 2028-04-17
```

**2년이 지나면 조용히 멈춥니다.** 어느 날 갑자기 자료가 안 나오면 만료를 먼저 의심하십시오.

- 만료된 것은 `만료·중지 → 기간만료` 에 잡힙니다
- **활용연장** 을 신청하면 계속 쓸 수 있습니다
- 여러 개를 같은 날 신청했다면 **같은 날 한꺼번에 만료됩니다.** 만료예정일을 달력에 적어두십시오

---

## 자주 막히는 곳

| 증상 | 원인 |
|---|---|
| 인증 오류가 난다 | Encoding / Decoding 키를 반대로 넣었습니다 |
| 신청했는데 안 된다 | `마이페이지 → Open API → 활용신청 현황` 에서 **승인** 상태인지 확인하십시오 |
| 키는 있는데 특정 자료만 안 된다 | 그 API에 **활용신청을 안 한 것**입니다. 키가 아니라 신청 문제입니다 |
| 어제는 됐는데 오늘 안 된다 | 하루 한도를 넘겼을 수 있습니다. 다음 날 확인하십시오 |
| 파일만 받아진다 | **파일데이터**를 신청했습니다. **오픈 API** 로 다시 찾으십시오 |
| 활용기간이 끝났다 | `만료·중지` 에 잡힙니다. **활용연장** 을 신청하십시오 |

---

## 인증키를 다룰 때

인증키는 **비밀번호와 같습니다.**

- 화면 공유·발표 자료·단체 대화방에 노출하지 마십시오
- 남에게 알려주지 마십시오. 그 사람이 쓴 만큼 내 한도가 깎입니다
- 노출됐다면 포털에서 **재발급**받으십시오

## 참고

- 공공데이터포털 — `https://www.data.go.kr`
- 문의 — `opendata_help@nia.or.kr` · 1566-0025
