# Claude in Chrome 설치 안내

Claude 가 **내가 보는 화면을 같이 봅니다.**
복사해서 붙여넣는 왕복이 없어지고, Claude 가 읽고·누르고·입력하고·양식까지 채웁니다.

방법이 두 가지입니다. **둘 중 하나만 있으면 됩니다.**

| | ① 크롬 확장 (Claude in Chrome) | ② 내장 브라우저 (Cowork) |
|---|---|---|
| 설치 | 크롬 웹스토어에서 확장을 깝니다 | **설치할 것이 없습니다.** 데스크탑 앱에 이미 들어 있습니다 |
| 어디서 열리나 | 내가 쓰던 크롬 창 | 데스크탑 앱 옆에 붙는 패널 |
| 로그인 | 내가 이미 로그인해 둔 계정 그대로 | 따로 로그인해야 합니다 |
| 언제 좋은가 | **지금 눈앞에 열어 둔 페이지**를 다룰 때 | 웹 작업을 맡겨 두고 딴 일을 할 때 |
| 브라우저 | **크롬에서만** 됩니다 | 크롬을 안 써도 됩니다 |

> [!IMPORTANT]
> **요금제는 둘 다 유료 플랜만** 됩니다 (Pro · Max · Team · Enterprise). 무료는 안 됩니다.
> 크롬 확장은 **모바일과 엣지·웨일에서 안 됩니다.**

---

## ① 크롬 확장 — 설치 6단계

1. 크롬을 엽니다
2. 크롬 웹스토어의 Claude 확장 프로그램 페이지로 갑니다

```
https://chromewebstore.google.com/detail/claude/fcoeoabgfenejglbffodgkkbkcdhcgfn
```

3. `Chrome에 추가` 를 누릅니다
4. Claude 계정으로 로그인합니다
5. 주소창 오른쪽 **퍼즐 조각 아이콘 → 압정(고정)** 을 눌러 툴바에 고정합니다
6. 권한 요청이 뜨면 허용합니다

**이렇게 되면 성공** — 크롬 툴바에 Claude 아이콘이 보입니다.
**아이콘을 누르면 사이드 패널이 열립니다.** 여기까지는 별도 설정이 없습니다.

- 사이드 패널은 **「자동 승인」 모드로 시작**합니다 (Max·Team 기준, Pro 는 순차 배포).
  모드를 바꾸면 **다음 세션에도 그 선택이 유지**됩니다.
- 클래식 화면으로 되돌리려면 점 3개 → `Switch back to classic`.
- **워크플로 녹화는 클래식에서만** 됩니다.

---

## ② 내장 브라우저 — 깔 것이 없습니다

Cowork 작업이 웹사이트를 건드리면 **데스크탑 앱 옆에 브라우저 패널이 저절로 열립니다.**
Claude 가 사이트를 열고·읽고·누르고·입력하는 것을 **보면서 기다리면 됩니다.**

할 수 있는 일은 크롬 확장과 같습니다. **내 브라우저를 쓰지 않으므로 내 탭과 로그인을 건드리지 않습니다.**

> [!WARNING]
> **로그인이 따라오지 않습니다.** 내 크롬에 로그인돼 있어도 내장 브라우저는 남입니다.
> 처음 열릴 때 「브라우저에서 쿠키 가져오기」를 물어보는데,
> **윈도우에서는 파이어폭스에서만** 가져올 수 있습니다. 크롬에서 가져오기는 맥 전용입니다.
> 윈도우에서 크롬만 쓰신다면 **필요한 사이트마다 그때그때 로그인**하시면 됩니다.

- 데스크탑 앱이 **켜져 있고 인터넷에 연결돼 있어야** 동작합니다.
- 윈도우·맥·리눅스(베타), Pro·Max·Team 에서 순차 배포 중입니다. 안 보이면 며칠 뒤 다시 보십시오.

---

## 어느 것을 쓸지 정하는 곳

**설정 → `Cowork` → `기본 브라우저`** 에서 「내장 브라우저」 또는 「Chrome (Claude in Chrome)」 을 고릅니다.

- **크롬 확장이 이미 깔려 있으면 그쪽이 계속 기본**입니다.
- 확장이 없으면 **내장 브라우저가 기본**입니다.
- 정해 둔 쪽을 쓰되, 대화 중에 **「내장 브라우저로 해줘」처럼 말하면 그때만 바꿔 씁니다.**

> [!NOTE]
> 예전의 **`설정 → 커넥터 → Claude in Chrome → 구성 → 토글`** 경로는 지금 없습니다.
> 위의 `설정 → Cowork → 기본 브라우저` 가 그 자리를 대신합니다.

---

## 조심할 것

- Claude 가 **로그인된 사이트도 만집니다.** 켜기 전에 무슨 탭이 열려 있는지 보세요.
- **내장 브라우저에 한 번 로그인하면 다음 세션에도 Claude 가 그 계정을 씁니다.**
  은행·증권·메일처럼 **돈과 개인정보가 걸린 사이트는 로그인하지 마세요.**
- 결제·송금처럼 **돈이 움직이는 일은 직접 하세요.** Claude 에게 시키지 마세요.
- 웹페이지 안에 숨겨둔 지시문으로 Claude 를 속이는 공격이 있습니다.
  **아는 사이트부터 쓰고, 이상하면 즉시 중지**하세요.

---

출처 (2026-09-08 직접 조회)

- support.claude.com/en/articles/12012173 — Claude in Chrome 시작하기
- support.claude.com/en/articles/16607400 — Cowork 내장 브라우저
