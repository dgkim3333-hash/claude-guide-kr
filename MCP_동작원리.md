# MCP 는 어떻게 도는가 — 설정 파일에 적으면 무슨 일이 벌어지나

> 이 문서는 **MCP 를 붙이기 전에 한 번 읽어두시면 좋은 글**입니다.
> 붙이는 방법이 아니라 **왜 그렇게 해야 하는지**를 다룹니다.
>
> 여기를 이해하고 나면 「빨간불이 왜 뜨는지」, 「왜 껐다 켜야 하는지」,
> 「키를 넣었는데 왜 답이 비어 오는지」가 전부 설명됩니다.

---

## 한 문장으로

**MCP 는 자동 연결이 아니라, 설정 파일에 적힌 프로그램을 Claude 가 실제로 실행하는 것입니다.**

많은 분이 「config 에 적고 API 키를 넣으면 알아서 연결된다」고 생각하십니다.
그렇지 않습니다. **Claude 가 사장님 컴퓨터에서 프로그램을 직접 켜는 것**입니다.

---

## 직원을 출근시킨다고 생각하시면 됩니다

| 실제 | 비유 |
|---|---|
| `claude_desktop_config.json` | **직원 명단** — 누구를 어떻게 출근시킬지 적은 종이 |
| MCP 서버 하나 | **직원 한 명** — 내 컴퓨터에서 도는 작은 프로그램 |
| 도구 목록 | 그 직원이 **할 수 있는 일 목록** |
| API 키 | 직원이 바깥에서 내미는 **회원증** |

---

## 다섯 관문 — 순서대로 통과해야 합니다

```
① 실행    설정 파일을 읽고 프로그램을 켠다      → 여기서 막히면 빨간불
② 명함    프로그램이 할 수 있는 일 목록을 낸다   → 여기까지는 키가 필요 없다
③ 선택    Claude 가 알맞은 도구를 고른다
④ 인증    프로그램이 바깥에 키를 내밀고 자료를 받는다  → 여기서 막히면 빈 답
⑤ 답변    Claude 가 받은 자료로 답한다
```

### ① Claude 앱을 켤 때 — 프로그램을 실제로 실행합니다

Claude 가 켜지면 설정 파일을 읽고, 적힌 명령어를 **그대로 실행합니다.**

```json
"command": "npx",  "args": ["-y", "firecrawl-mcp"]
```

이렇게 적혀 있으면 Claude 는 `npx -y firecrawl-mcp` 를 칩니다.
**사장님이 검은 창에 직접 치는 것과 똑같은 일**입니다. 창이 안 보일 뿐입니다.

서버 8개면 프로그램 8개가 Claude 창 뒤에서 같이 돕니다.
작업 관리자를 열어보시면 `node.exe` 나 `python.exe` 가 그만큼 떠 있습니다.

> **여기서 잘못되면 그 서버만 빨간불입니다.** 명령어를 못 찾거나(`uv` 없음),
> 폴더가 비었거나, 설정 파일 글씨가 틀렸을 때입니다.
> **Claude 자체는 멀쩡히 켜집니다.** 그래서 눈치채기 어렵습니다.

### ② 출근한 프로그램이 명함을 냅니다

실행된 프로그램은 곧바로 Claude 에게 **자기가 할 수 있는 일 목록**을 보냅니다.

```
legal_dong_code_search — 주소로 법정동코드를 찾는다
realprice_apt_trade    — 아파트 매매 실거래가를 가져온다
...
```

이 목록이 곧 **「도구」**입니다. 설정 화면에서 서버 이름 옆에 뜨는 숫자가 이것입니다.

> **★ 여기에는 키가 필요 없습니다.** 명함만 내미는 것이라 인증이 없습니다.
> **그래서 키가 틀려도 이름과 도구 개수는 멀쩡하게 보입니다.**
> 「목록에 떠 있으니 연결된 것」이라고 착각하기 딱 좋은 지점입니다.

### ③ 질문하면 Claude 가 도구를 고릅니다

「오산시 원동 법정동코드 알려줘」라고 하면, Claude 가 목록을 훑어 알맞은 도구를 고르고
넣을 값까지 스스로 정합니다.

```
legal_dong_code_search(keyword="오산시 원동")
```

**도구 이름을 외울 필요가 없는 이유가 이것입니다.** 한국말로 말하면 Claude 가 알아서 고릅니다.
목록에 적힌 설명을 읽고 판단하므로, **설명이 잘 쓰인 MCP 일수록 잘 골라냅니다.**

잘 못 고를 때는 「datagokr 로 찾아줘」처럼 서버 이름을 직접 말해주시면 됩니다.

### ④ 프로그램이 바깥에 전화합니다 — 키를 쓰는 곳

이제 그 프로그램이 공공데이터포털 같은 바깥 서비스에 접속합니다. **키를 함께 보냅니다.**

```
apis.data.go.kr/... ?serviceKey=내키&keyword=오산시 원동
```

**키가 쓰이는 곳은 오직 여기 한 군데입니다.** ①②③ 어디에도 안 쓰입니다.

> **여기서 잘못되면 증상이 ①과 다릅니다.** 빨간불이 아닙니다.

| 무엇이 틀렸나 | 어떻게 보이나 |
|---|---|
| 키가 틀림 | 이름은 뜨는데 **인증 오류** |
| Encoding 키를 넣음 | 같은 증상 — 가장 흔합니다 |
| **활용신청을 안 함** | **답은 오는데 내용이 비어 있음** |
| 활용기간 만료 | 잘 되던 게 **어느 날 조용히 멈춤** |

### ⑤ Claude 가 정리해서 답합니다

받아온 자료를 읽고 질문에 맞게 정리합니다.

**이 숫자는 Claude 가 지어낸 것이 아닙니다.** ④에서 실제로 받아온 값입니다.
**연결이 없으면 그럴듯하게 지어내고, 연결이 있으면 원문을 읽고 답한다**는 말이 이 뜻입니다.

---

## 왜 어떤 건 폴더가 필요하고 어떤 건 안 필요한가

설정 파일에 적는 방식이 두 가지입니다.

| 적는 법 | 뜻 | 폴더 |
|---|---|---|
| `npx -y firecrawl-mcp` | **파견업체에 전화해서 보내달라** — 인터넷에서 그때그때 받아옴 | **불필요** |
| `uv --directory C:\AI\mcp\datagokr-mcp ...` | **우리 건물 그 방 사람** — 내 컴퓨터에 미리 있어야 함 | **필요** |

`--directory` 줄은 이렇게 읽으시면 됩니다.

> 「**그 폴더로 들어가서 `server.py` 를 실행해라**」

그 폴더가 없으면 실행할 파일이 없습니다. **설정 파일만 고쳐서는 절대 안 되는 이유**입니다.

**Firecrawl 이 쉬운 이유가 여기 있습니다.** `npx` 가 ①을 알아서 해결하므로 ④만 하시면 됩니다.
**datagokr 이 어려운 이유도 같습니다.** ①부터 손으로 해야 합니다.

---

## `uv sync` 는 무엇을 하나

폴더가 필요한 MCP 는 **폴더만 있어서도 안 됩니다.**

파이썬 프로그램은 혼자 못 돕니다. `server.py` 맨 위를 보면 이런 줄이 있습니다.

```python
import httpx
from fastmcp import FastMCP
```

「남이 만든 부품 `httpx` 와 `fastmcp` 를 가져다 쓰겠다」는 뜻입니다.
**그 부품은 폴더 안에 없습니다. 인터넷에서 받아야 합니다.**

세 가지가 짝을 이룹니다.

| 파일 | 역할 |
|---|---|
| `server.py` | **조립 설명서** — 무엇을 어떻게 할지 |
| `pyproject.toml` | **부품 주문서** — 무엇이 필요한지 |
| `.venv\` | **부품 창고** — `uv sync` 가 만들어 줍니다 |

**`uv sync` 는 주문서를 읽고 부품을 받아다 `.venv` 에 넣는 명령입니다.**

- 부품 하나가 또 다른 부품을 부르기 때문에, **2개를 주문했는데 28개가 깔립니다.** 정상입니다
- **처음 한 번만 2~3분** 걸립니다. 두 번째부터는 몇 초입니다
- **프로그램마다 창고를 따로 둡니다.** A는 2.0, B는 3.0 이 필요할 수 있어서입니다
- **`.venv` 를 지워도 괜찮습니다.** `uv sync` 를 다시 돌리면 복구됩니다
- `uv` 는 배달 담당자 이름입니다. 예전 `pip` 과 하는 일은 같고 훨씬 빠릅니다

> **「폴더는 넣었는데 왜 안 되지?」의 대부분이 `uv sync` 를 빠뜨린 것입니다.**

---

## 왜 껐다 켜야 하나

**설정 파일은 출근 시간에만 읽습니다.**

근무 중에 명단을 고쳐도 이미 출근한 사람은 안 바뀝니다.
그래서 설정 파일을 고치면 **Claude 를 완전히 종료했다가 다시 켜야** 합니다.

**창의 `X` 로는 안 꺼집니다.** 시계 옆 트레이 아이콘에서 오른쪽 클릭 → 종료까지 하셔야 합니다.

---

## 증상으로 원인 찾기

| 증상 | 어느 관문 | 원인 |
|---|---|---|
| 설정 화면에 서버 이름이 아예 없다 | ① | 설정 파일 글씨·쉼표 오류 |
| 이름은 있는데 빨간불 | ① | 폴더 없음 · `uv sync` 안 함 · 경로 오타 · `uv` 미설치 |
| 이름도 도구도 멀쩡한데 답이 안 온다 | ④ | 키가 틀림 (Encoding/Decoding 확인) |
| 답은 오는데 내용이 비어 있다 | ④ | **활용신청 미승인** |
| 잘 되던 게 어느 날 멈췄다 | ④ | 키 활용기간 만료 |
| 도구가 있는데 Claude 가 안 쓴다 | ③ | 서버 이름을 직접 말해보세요 |

**막히면 그 화면을 캡처해서 Claude 에게 보여주십시오.**
「MCP 가 안 붙어요」라고만 말해도 [MCP 점검 스킬](mcp-health-check.skill)이
설정 파일과 로그를 대신 읽어 짚어줍니다.

---

## 정리

- MCP 는 **자동 연결이 아니라 프로그램 실행**입니다
- **키는 마지막 관문(④)에서만** 쓰입니다. ①이 먼저입니다
- **`npx` 는 폴더가 필요 없고**, `--directory` 는 필요합니다
- 폴더가 필요한 것은 **`uv sync` 까지 해야** 돕니다
- **빨간불은 ①, 빈 답은 ④** — 증상이 다르면 원인도 다릅니다
- 설정을 고치면 **완전 종료 후 재시작**
