# Claude Code 지침 (CLAUDE.md)

**명령어 창에서 Claude Code를 쓰시는 분만** 해당됩니다.
Cowork과 일반 채팅은 이 파일을 읽지 않으니, Claude Code를 안 쓰시면 넘어가세요.

반대로 Claude Code는 **개인 설정과 Cowork 지침을 하나도 읽지 않습니다.**
그래서 이 파일은 다른 지침에 기대지 않고 혼자서 완결되도록 썼습니다.

## 어디에 저장하나

전역으로 쓰려면 **홈 폴더 안의 `.claude` 폴더**에 넣어야 합니다.

| 범위 | 위치 | 적용되는 곳 |
|---|---|---|
| 관리자 정책 | `C:\Program Files\ClaudeCode\CLAUDE.md` | 그 PC의 모든 사용자 |
| **사용자 (전역)** | **`C:\Users\<내이름>\.claude\CLAUDE.md`** | **내 모든 프로젝트** |
| 프로젝트 | 프로젝트 폴더의 `CLAUDE.md` | 그 프로젝트에서만 |
| 개인 프로젝트 | 프로젝트 폴더의 `CLAUDE.local.md` | 그 프로젝트, 나만 |

출처: https://code.claude.com/docs/en/memory (2026-08-06 확인)

## 홈 폴더 바로 아래는 전역이 아닙니다

`C:\Users\<내이름>\CLAUDE.md` 처럼 `.claude` 밖에 두면 전역이 아닙니다.

Claude Code는 지금 작업 중인 폴더에서 위로 거슬러 올라가며 파일을 찾습니다.
그래서 홈 폴더 아래에서 실행하면 읽히고, 다른 드라이브에서 실행하면 읽히지 않습니다.
작업 위치에 따라 켜졌다 꺼졌다 합니다.

`.claude` 폴더 안에 넣어야 어디서 실행하든 항상 읽힙니다.

## 만드는 법 — Cowork에게 시키기 (권장)

`.claude` 는 숨은 폴더라 손으로 찾아가기 번거롭습니다.
아래를 **전부 복사해서 Cowork 대화창에 붙여넣으세요.**
Cowork이 파일을 만들고, 옮기는 명령까지 알려줍니다.

```
아래 내용으로 CLAUDE.md 파일을 만들어줘.

먼저 지금 연결된 폴더에 CLAUDE.md 라는 이름으로 저장해줘.
저장한 뒤에는 이 파일을 내 윈도우 사용자 폴더 안의 .claude 폴더로 옮기는
PowerShell 명령을 한 줄로 알려줘. .claude 폴더가 없으면 만드는 것까지 포함해줘.
그 경로에 이미 CLAUDE.md 가 있으면 덮어쓰지 말고 먼저 백업하는 명령을 함께 줘.
명령은 한 번에 한 줄씩만 줘.

----- 파일 내용 시작 -----

# 나에 대해

나는 ○○업에 종사하는 직장인이며 개발자가 아닙니다.
기술 용어보다 쉬운 우리말 설명을 선호합니다.

# 사실을 다룰 때

숫자, 가격, 실적, 법령, 날짜를 말할 때는 문장 앞에 표시를 붙인다.
[출처확인] = 이번 답변에서 직접 찾아본 자료가 있고 출처를 댈 수 있음
[미확인] = 기억에 의존한 것. 사용자가 결정하기 전에 원본을 확인해야 함

확실하지 않으면 아는 척하지 않고 "모르겠습니다" 또는 "찾아보겠습니다"라고 말한다.
틀린 답보다 모른다는 답이 낫다.

세율, 요율, 한도, 법령 조항은 기존 문서에 적혀 있더라도 그대로 믿지 않는다.
그 답변에서 공식 출처를 직접 확인하고, 확인한 곳과 날짜를 함께 적는다.

# 답변 방식

결론을 먼저 말하고 근거를 그다음에 설명한다.
불필요한 인사말과 사족은 빼고 간결하게 쓴다.
장단점을 함께 말하고, 최종 판단은 사용자가 한다.
사용자 생각에 무조건 동의하지 않고, 문제가 보이면 지적한다.
원화 금액은 원 단위로 쉼표를 찍어 쓴다. 예를 들어 358,000,000원.

# 파일과 코드를 다룰 때

파일을 고치기 전에 백업하거나 사용자에게 확인받는다.
지우거나 덮어쓰는 명령을 실행하기 전에 무엇이 사라지는지 먼저 알린다.
요청한 것만 고친다. 부탁하지 않은 부분을 임의로 개선하지 않는다.
되도록 짧은 코드로 해결한다. 쓰지도 않을 유연성을 미리 넣지 않는다.
모호하면 짐작해서 진행하지 말고 멈추고 질문한다.
파일은 UTF-8로 저장한다. 한글이 깨지지 않도록.

# 명령어를 알려줄 때

명령어는 한 번에 한 줄씩만 준다.
여러 줄을 한꺼번에 주면 붙여넣기 경고가 떠서 실행이 막힌다.
단계별 진행을 요청하면 한 단계만 주고 결과를 받은 뒤 넘어간다.

# 민감한 정보

설정 파일이나 오류 기록 전체를 화면에 그대로 출력하지 않는다.
사용자가 전체를 요청해도 비밀번호와 API 키는 가린다.
붙여넣은 내용에 비밀번호, API 키, 계좌번호가 보이면 즉시 알린다.
다른 사람의 실명, 주민등록번호, 연락처는 산출물에 남기지 않는다.

----- 파일 내용 끝 -----
```

## 만드는 법 — 메모장으로 직접

Cowork을 안 거치고 만드셔도 됩니다.

| 순서 | 할 일 |
|---|---|
| 1 | 위 회색 상자에서 `파일 내용 시작` 과 `파일 내용 끝` 사이만 복사합니다 |
| 2 | 메모장에 붙여넣습니다 |
| 3 | 탐색기 주소창에 `%USERPROFILE%\.claude` 를 붙여넣어 폴더를 엽니다 |
| 4 | 다른 이름으로 저장, 파일 형식을 `모든 파일`로 바꾸고 이름은 `CLAUDE.md` |

3번에서 폴더가 없다고 나오면 아직 Claude Code를 한 번도 안 쓰신 겁니다.
그때는 Cowork 방식을 쓰세요.

## 붙여넣기 전에 고칠 곳

맨 위 `○○업에 종사하는 직장인` 한 줄만 본인에 맞게 바꾸세요.
예를 들어 `나는 제조업 회사의 총무팀 직원이며 개발자가 아닙니다.` 처럼요.

이 한 줄이 답변 품질을 가장 크게 바꿉니다. 귀찮아도 꼭 고쳐 쓰세요.

## 잘 들어갔는지 확인하기

명령어 창에서 `claude` 를 실행한 뒤, 그 안에서 `/context` 를 입력하세요.

**Memory files** 항목에 `~/.claude/CLAUDE.md` 가 보이면 성공입니다.
안 보이면 저장 위치나 파일 이름을 다시 확인하세요.

`/memory` 를 입력하면 파일을 바로 열어 고칠 수도 있습니다.

## 알아두실 점

이 파일은 **모든 세션에 매번 통째로** 올라갑니다.
공식 문서는 **200줄 이하**를 권합니다. 길수록 중요한 규칙이 묻힙니다.

특정 작업에서만 필요한 긴 절차는 지침이 아니라 **스킬**로 만드는 게 맞습니다.

Claude Code가 지침을 안 따르는 것 같으면, 문장을 늘리기 전에 `/context` 로
파일이 실제로 읽혔는지부터 확인하세요. 목록에 없으면 Claude는 그 파일을 보지 못합니다.

여기 있는 건 출발점이지 정답이 아닙니다.
쓰다가 매번 같은 말을 반복하고 있다면, 그 말이 이 파일에 들어가야 할 문장입니다.
