# 경매 물건 분석 — Claude 프로젝트 배포본

경매 서류 PDF를 넣으면 **권리분석 → 임차인 판정 → 예상 배당 → 세금 → 명도까지**
한 번에 검토하고 **PPTX 물건카드 1부**를 만들어 주는 Claude 프로젝트입니다.

> ⚠️ **이 도구는 판단 재료를 모아줄 뿐, 대신 결정하지 않습니다.**
> 실제 입찰 전에는 등기부·매각물건명세서 원문과 전문가 확인이 반드시 필요합니다.
> 투자 판단과 그 결과는 본인 책임입니다.

---

## 설치 — 3단계

### 1단계. 폴더 만들기

내 컴퓨터 아무 곳에나 물건을 담을 폴더를 만듭니다. 예를 들면 이렇게요.

```
C:\경매\
```

그 아래 물건마다 폴더를 하나씩 만듭니다. **소재지로 이름을 짓습니다.**

```
C:\경매\평택 고덕동 1990-13\
C:\경매\화성 남양읍 2274-13\
```

사건번호로 짓지 않습니다 — 재경매로 사건번호가 바뀌어도 물건은 그대로이기 때문입니다.

### 2단계. 프로젝트 만들기

Claude 데스크탑을 엽니다.

1. 왼쪽 사이드바에서 **`프로젝트`** → **`새 프로젝트`**
2. 이름을 이렇게 넣습니다 → `경매 물건 분석`
3. **`지침 편집`** 을 누릅니다
4. 이 폴더의 **`프로젝트지침.md`** 파일을 메모장으로 열고 `Ctrl+A` → `Ctrl+C` → 지침 칸에 `Ctrl+V`
5. 프로젝트에 1단계에서 만든 폴더(`C:\경매`)를 연결합니다

### 3단계. 스킬 넣기 — 복사해서 붙여넣기만 하면 됩니다

**스킬은 파일을 설치하는 게 아니라, 문서를 통째로 복사해 대화창에 붙여넣어 만듭니다.**
아래 2개가 그 문서입니다. 파일 하나가 스킬 하나입니다.

| 순서 | 스킬 문서 | 하는 일 | 우선순위 |
|---|---|---|---|
| 1 | [`스킬_물건카드.md`](스킬_물건카드.md) | **주 스킬.** 서류 → 분석 → PPTX 물건카드 | 필수 |
| 2 | [`스킬_주거용경매.md`](스킬_주거용경매.md) | 다가구·다세대·아파트 GO/NO-GO 판단 | 권장 |

**1번 하나만 넣어도 기본 분석은 됩니다.**

#### 넣는 방법 (둘 다 같습니다)

1. 위 표에서 문서 하나를 클릭해 엽니다
2. 오른쪽 위 **복사 버튼**으로 파일 전체를 복사합니다
3. Claude 데스크탑 대화창에 붙여넣습니다
4. 맨 아래에 한 줄 덧붙이고 엔터를 칩니다

```
위 내용을 그대로 스킬로 저장해줘. 요약하거나 바꾸지 마.
```

5. 스킬이 저장되면 **새 대화**부터 작동합니다

**이렇게 되면 성공** — 새 대화에서 `이 물건 분석해줘` 라고 쳤을 때 분석이 시작됩니다.
각 문서 맨 위에 그 스킬의 확인 문구가 적혀 있으니 그대로 쳐 보세요.

> 여기서 많이 막힙니다. **붙여넣은 그 대화에서는 스킬이 아직 안 뜹니다.** 새 대화를 여세요.

#### ⚠️ 두 스킬이 겹칩니다 — 헷갈리기 쉬운 곳

`스킬_물건카드` 와 `스킬_주거용경매` 는 **권리분석·임차인 판정이 겹칩니다.**
둘 다 깔려 있으면 상황에 따라 어느 쪽이 뜰지 모릅니다. 이렇게 나눠서 쓰세요.

| 상황 | 쓰는 스킬 | 나오는 것 |
|---|---|---|
| 서류(매각물건명세서·등기부)가 있다 | `스킬_물건카드` | PPTX 물건카드 |
| 서류 없이 물건 종류로 판단만 본다 | `스킬_주거용경매` | 100점 채점 + GO/NO-GO |

원하는 쪽이 안 뜨면 **스킬 이름을 직접 불러 주세요.** 예 — `residential-auction 으로 봐줘`

---

## 여기에 없는 것 — 미리 알아두세요

### 입찰가를 자동으로 뽑아 주는 스킬은 배포하지 않습니다

`스킬_물건카드` 를 쓰다 보면 **「입찰가는 auction-bid-price 로 넘긴다」** 는 문장이 나옵니다.
그 스킬은 **이 저장소에 없습니다.**

강사가 직접 모은 낙찰 실측 자료와 실제 입찰 사례로 만든 것이라 공개하지 않습니다.
**얼마를 쓸지는 스스로 정하셔야 합니다.** 스킬이 대신 정해 주지 않습니다.

물건카드에 나오는 시세·배당·인수 부담을 근거로 삼되, **인근 낙찰 사례를 직접 찾아 대조**하세요.
법원경매정보와 온비드에서 같은 지역·같은 용도의 최근 낙찰가를 보는 것이 출발점입니다.

이 부분은 **수업 시간에 따로 다룹니다.**

### 채무자가 법인일 때 회생·파산을 확인하는 스킬도 없습니다

같은 이유로 빠졌습니다. 법인 채무자를 만나시면 **법원 나의사건검색과 대법원 공고**를 직접 확인하세요.

### `skills` 폴더는 무엇인가요

붙여넣기 방식으로 바꾸기 전에 쓰던 **`.skill` 파일**들입니다. 설정에서 파일로 추가하는 옛 방식입니다.
**새로 배우시는 분은 열지 않아도 됩니다.** 위 2개 문서로 충분합니다.

다만 **`post-auction-eviction`(낙찰 후 명도) 하나는 아직 여기에만 있습니다.**
이 스킬은 워드 서식 8종과 계산 스크립트가 본체라, 붙여넣기로는 서류가 만들어지지 않습니다.
명도 단계까지 가시면 그때 파일로 넣으세요.

---

## MCP 연결

MCP를 하나도 안 붙여도 **서류만 가지고 분석은 됩니다.** 다만 시세·법령·공시지가를 직접 못 가져와서
"확인이 필요합니다"가 많아집니다.

무엇을 붙이면 무엇이 좋아지는지는 **`필요한MCP.md`** 를 보세요.
수업 시간에 강사가 같이 붙여 줍니다.

---

## 첫 사용법

물건 폴더에 서류 PDF를 넣고, 프로젝트 안에서 새 대화를 열어 이렇게 말합니다.

```
이 물건 분석해줘
```

넣으면 좋은 서류 (많을수록 정확해집니다):

- **매각물건명세서** ← 이게 없으면 분석의 절반이 날아갑니다
- 현황조사서
- 전입세대열람
- 감정평가서
- 등기사항전부증명서 (토지·건물 **각각**)
- 건축물대장
- 문건/송달내역
- 경매정보 출력물

---

## 자주 쓰는 말

- `이 물건 분석해줘`
- `물건카드 만들어줘. 개인·법인 두 경우로 세금 넣어줘`
- `공시가격 000원으로 확인됐어. 보유세 다시 산출해줘`
- `임차인이 인수되는지만 다시 봐줘`
- `이 다가구 GO 인지 NO-GO 인지 판단해줘`

---

## 알아두실 것

**공시가격은 감정평가액의 50%로 가정합니다.**
주택 공시가격을 개인이 조회할 수 있는 공개 API가 없기 때문입니다.
정확한 값이 필요하면 [부동산공시가격알리미](https://www.realtyprice.kr) 에서 확인해 알려주세요. 즉시 다시 계산합니다.

**종부세는 낙찰 다음 해가 첫 과세연도입니다.**
과세기준일이 6월 1일이라, 6월 이후 잔금을 내면 그 해 종부세는 없습니다.
그래서 현행 세율이 아니라 **2027년 이후 세율**로 계산합니다.
2026.08.03 재정경제부 세제개편안 기준이며 **국회 통과 전 정부안**입니다.

**민간 추정시세는 쓰지 않습니다.**
국토교통부 실거래가만 씁니다. 경매정보 사이트의 AI 추정가도 쓰지 않습니다.

---

## 이 폴더의 갱신에 대하여

**여기 있는 스킬 문서는 자동으로 갱신되지 않습니다.**
강사 PC의 스킬을 손으로 복사해 올린 것이라, 강사가 스킬을 고쳐도 여기는 그대로 남습니다.
아래 날짜가 오래됐으면 최신판이 아닐 수 있습니다.

**마지막 갱신: 2026-08-16**
