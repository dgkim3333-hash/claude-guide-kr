[project]
name = "vworld-landuse-mcp"
version = "0.1.0"
description = "브이월드 토지이용계획·개별공시지가 조회 MCP 서버"
requires-python = ">=3.10"
dependencies = [
    "mcp<2",
    "httpx>=0.27",
]

[tool.uv]
package = false
