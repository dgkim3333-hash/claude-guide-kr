# 사전 준비 — Node.js와 uv 설치

MCP를 붙이려면 두 가지가 먼저 깔려 있어야 합니다.
이게 없으면 설정 파일을 아무리 잘 넣어도 MCP가 **하나도 뜨지 않습니다.**

## 왜 두 개인가

MCP는 만든 언어에 따라 실행 방식이 다릅니다.
설정 파일에서 `"command"` 값만 보면 무엇이 필요한지 알 수 있습니다.

| `"command"` 값 | 무엇으로 만든 MCP | 필요한 것 |
|---|---|---|
| `"npx"` | 자바스크립트 | **Node.js** |
| `"uvx"` 또는 `"uv"` | 파이썬 | **uv** |

`uv`는 파이썬 프로그램을 대신 받아서 실행해 주는 도구입니다.
`npx`가 자바스크립트 쪽에서 하는 일을, `uv`가 파이썬 쪽에서 합니다.

---

## 1. Node.js 설치

<https://nodejs.org/ko/download> 에 접속합니다.

- **초록색 LTS 버튼**으로 받으세요. 최신 버전 말고 LTS입니다.
- 설치 중 나오는 선택지는 **전부 기본값 그대로** 두고 다음만 누르시면 됩니다.

### ★ 체크박스 하나만 조심하세요 — 여기서 30분이 날아갑니다

설치가 거의 끝날 무렵 이런 체크박스가 나옵니다.

```
Automatically install the necessary tools for Native Modules
```

**체크하지 마세요.** 기본값(꺼짐) 그대로 두고 넘어가시면 됩니다.

「필요한 도구(necessary tools)」라고 적혀 있어서 켜야 할 것 같지만, **이 교육에는 필요 없습니다.**
C나 C++로 만든 프로그램을 내 컴퓨터에서 직접 번역(컴파일)할 때 쓰는 개발자용 도구입니다.
우리가 붙이는 MCP 는 전부 이미 만들어진 것을 받아서 실행하므로 번역할 일이 없습니다.

체크하면 설치가 끝난 뒤 **검은 창이 뜨면서** 이런 문구가 나옵니다.

```
This script will install Python and the Visual Studio Build Tools...
This will require about 7 GiB of free disk space...
```

**파이썬 · 비주얼 스튜디오 빌드 도구 · Chocolatey · 윈도우 업데이트**를 전부 깔기 시작합니다.
디스크 7GB 이상을 쓰고 수십 분이 걸리며, 재부팅을 요구하기도 합니다.

### 이미 그 검은 창을 보셨다면

**그냥 창을 닫으세요.** 화면 아래쪽에도 `You can close this window to stop now` 라고 적혀 있습니다.
**Node.js 자체는 이미 설치가 끝난 상태**입니다. 이건 덤으로 딸려 나온 별개 스크립트일 뿐입니다.

중간에 닫아도 컴퓨터에 문제가 생기지 않습니다. 이미 다 깔렸더라도 해로운 것은 아니고
공간만 차지합니다. 정리하고 싶으시면 `설정 → 앱` 에서 **Chocolatey** 와
**Visual Studio Build Tools** 를 지우시면 됩니다.

창을 닫으신 뒤 아래 §3 「설치 확인」으로 넘어가 `node -v` 만 확인하시면 됩니다.

---

## 2. uv 설치

### 먼저 — 검은 창은 두 종류입니다

여기서 가장 많이 막힙니다. 생김새가 거의 같은데 **문법이 다릅니다.**

| | PowerShell | 명령 프롬프트 (cmd) |
|---|---|---|
| 창 제목 | Windows PowerShell | 명령 프롬프트 |
| 첫 줄 | `PS C:\Users\내이름>` | `C:\Users\내이름>` |
| 내 폴더를 뜻하는 말 | `$env:USERPROFILE` | `%USERPROFILE%` |

**앞에 `PS` 가 붙어 있는지만 보시면 됩니다.**

이 문서는 **PowerShell 기준**입니다. 명령 프롬프트에 PowerShell 문법을 넣으면 이렇게 나옵니다.

```
파일 이름, 디렉터리 이름 또는 볼륨 레이블 구문이 잘못되었습니다.
```

**설치 실패가 아닙니다.** 창을 잘못 고른 것입니다. PowerShell 을 열고 다시 하세요.

### PowerShell 열기

시작 메뉴에 `powershell` 을 치면 **Windows PowerShell** 이 나옵니다. 그것을 엽니다.

창에 이렇게 떠 있으면 제대로 연 것입니다.

```
PS C:\Users\내이름>
```

아래 한 줄을 복사해서 붙여넣고 엔터를 누릅니다.
(오른쪽 위 복사 버튼을 쓰시면 한 글자도 빠지지 않습니다)

```powershell
irm https://astral.sh/uv/install.ps1 | iex
```

---

## 3. 설치 확인

**PowerShell 창을 닫고 새로 연 다음** 아래 두 줄을 각각 넣어봅니다.

```powershell
node -v
```

```powershell
uv --version
```

`v22.20.0`, `uv 0.12.2` 처럼 버전 번호가 나오면 성공입니다.

> **★ `node -v` 가 `v22` 보다 낮게 나오면 다시 까셔야 합니다.**
> 예전에 깔아둔 Node.js 가 있으면 낮은 버전이 그대로 남아 있을 수 있습니다.
> 수업에서 붙이는 **Firecrawl 은 Node.js 22 이상**을 요구합니다 (`firecrawl-mcp` 3.24.0 기준).
> `v20` 이나 `v18` 이 나오면 위 §1 로 돌아가 **LTS 버튼**으로 다시 설치하십시오. 덮어써집니다.

> **성공 확인은 반드시 `uv --version` 으로 하세요.** 뒤에 나오는 `winget` 방식으로 깔면
> uv.exe 가 `%USERPROFILE%\.local\bin` 이 아닌 다른 곳에 들어갑니다.
> 그래서 폴더를 직접 들여다보는 확인은 **설치에 성공했는데도 실패로 나옵니다.**

---

## 4. 안 될 때

### "uv 를 찾을 수 없습니다"

원인이 **두 가지**입니다. 설치 자체가 안 됐거나, 설치는 됐는데 열려 있던 창이 예전 경로를 기억하고 있거나.
어느 쪽인지부터 가릅니다.

**① 파일이 실제로 깔렸는지 확인합니다**

PowerShell 이면

```powershell
dir "$env:USERPROFILE\.local\bin\uv.exe"
```

명령 프롬프트면

```
dir "%USERPROFILE%\.local\bin\uv.exe"
```

- 날짜와 크기가 나오면 **설치는 된 것**입니다. 창 문제이니 ②로 갑니다.
- 여기서도 "찾을 수 없습니다"가 나오면 **설치가 안 된 것**입니다. 위 2번 설치 명령을 다시 실행하세요.

**② 창을 완전히 닫고 새로 엽니다**

설치할 때 경로 등록은 끝났지만, 이미 열려 있던 창은 그 사실을 모릅니다.

**③ 컴퓨터를 다시 시작합니다**

②로 안 되면 재시작이 가장 확실합니다.

> 지금 당장 재시작을 못 하는 상황이면, **열려 있는 그 창에만** 임시로 경로를 알려줄 수 있습니다.
> 창을 닫으면 원래대로 돌아갑니다. 근본 해결은 ③입니다.
>
> PowerShell : `$env:Path += ";$env:USERPROFILE\.local\bin"`
> 명령 프롬프트 : `set PATH=%PATH%;%USERPROFILE%\.local\bin`

### "다른 프로세스에서 사용 중이므로 액세스할 수 없습니다"

이미 uv가 깔려 있는데 Claude 데스크탑이 그 파일을 쓰고 있는 중입니다.
**그대로 두셔도 됩니다.** 이미 깔린 uv가 정상 작동합니다.

굳이 최신으로 올리시려면

1. Claude 데스크탑을 완전히 종료합니다 (창만 닫지 말고 작업 표시줄 우측 트레이 아이콘까지)
2. 위 설치 명령을 다시 실행합니다
3. Claude 데스크탑을 다시 켭니다

### `>>` 만 계속 나오고 진행이 안 될 때

명령어를 중간까지만 붙여넣어서 PowerShell이 나머지를 기다리는 상태입니다.
`Ctrl + C` 를 눌러 빠져나온 뒤, 명령어를 **처음부터 끝까지** 다시 붙여넣으세요.

### 회사 PC라 보안 정책에 막힐 때

회사 PC는 **인터넷에서 받은 스크립트를 실행하지 못하게** 막아둔 곳이 많습니다.
그래서 위 2번의 짧은 설치 명령이 그대로는 안 먹힐 수 있습니다.

**① 실행 권한을 이번 한 번만 풀고 설치합니다**

uv 공식 문서가 Windows 에 안내하는 형태입니다. **한 줄 전체를 복사하세요.**

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**② 그래도 안 되면 winget 으로 깝니다**

```powershell
winget install --id=astral-sh.uv -e
```

**③ 확인**

창을 완전히 닫고 새로 연 다음

```powershell
uv --version
```

> **②로 깔았다면 폴더 확인이 아니라 `uv --version` 으로만 판정하세요.**
> winget 은 설치 위치가 달라서, 앞의 `dir "$env:USERPROFILE\.local\bin\uv.exe"` 가
> **성공했는데도 "찾을 수 없습니다"로 나옵니다.**

**①②가 모두 실패하면 — 여기서 멈추세요**

회사 정책·방화벽에 막힌 것입니다. 아래 문구가 나오면 그렇게 보시면 됩니다.

| 화면에 나온 말 | 무엇 |
|---|---|
| "이 시스템에서 스크립트를 실행할 수 없으므로…" | ByPass 를 붙였는데도 막힘 = 정책으로 고정돼 있음 |
| "이 언어 모드에서 지원되지 않습니다" | PowerShell 기능 자체를 제한해 둠 |
| "원격 서버에 연결할 수 없습니다" · SSL/TLS 오류 | 방화벽·프록시가 astral.sh 를 막음 |
| winget 쪽에 "정책" · "Group Policy" | 설치 자체를 금지해 둠 |

**개인이 우회하지 마세요. 회사 규정 위반이 될 수 있습니다.**
전산팀에 「astral.sh 접속과 uv 설치 허용」을 요청하시거나, 개인 PC 로 실습하세요.

> **헷갈리기 쉬운 두 가지 — 이건 보안 문제가 아닙니다.**
>
> - `'winget' 용어가 인식되지 않습니다` → winget 이 안 깔린 PC 입니다. ① 결과로만 판단하세요
> - `파일 이름, 디렉터리 이름 또는 볼륨 레이블 구문이 잘못되었습니다` → 명령 프롬프트에 넣은 것입니다. PowerShell 로 바꿔 다시 하세요

> **uv 가 없어도 대부분은 됩니다.** 채팅·개인설정·스킬·프로젝트·엑셀 실습은 uv 없이 전부 돌아갑니다.
> uv 가 필요한 것은 파이썬으로 만든 일부 MCP 뿐입니다.

---

## 5. 마지막으로 — Claude 데스크탑 재시작

Node.js나 uv를 새로 깔았다면 **Claude 데스크탑을 완전히 종료했다가 다시 켜야** 인식합니다.
창만 닫으면 뒤에서 계속 돌고 있습니다. 작업 표시줄 우측 트레이 아이콘까지 종료하세요.

---

## 다음 단계

설치가 끝났으면 MCP 를 연결합니다. 붙여넣기부터 확인까지 순서대로 안내합니다.
→ [Claude_MCP_연결_안내메시지.md](Claude_MCP_연결_안내메시지.md)

파일 안에 넣는 위치와 API 키 발급처가 전부 적혀 있습니다.

---

출처: <https://docs.astral.sh/uv/getting-started/installation/> · <https://nodejs.org/ko/download> (2026-08-07 확인)
uv 설치 위치 `%USERPROFILE%\.local\bin` : <https://docs.astral.sh/uv/reference/storage/> → Executable directory (2026-08-24 확인)
Windows 권장 설치 명령(`-ExecutionPolicy ByPass`) : <https://github.com/astral-sh/uv/blob/main/docs/getting-started/installation.md> 36행 (2026-08-24 확인)
