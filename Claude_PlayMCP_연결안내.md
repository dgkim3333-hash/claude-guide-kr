# PlayMCP 연결 안내

## PlayMCP란

카카오가 운영하는 **개방형 도구 연결 플랫폼**입니다.
카카오톡 나와의 채팅, 톡캘린더, 카카오맵, 선물하기, 멜론 같은 카카오 서비스와
외부 도구 약 200개가 등록되어 있습니다.

필요한 도구를 **도구함**에 담아두면, **카카오 계정 인증 한 번**으로 담긴 도구 전부를 Claude에서 쓸 수 있습니다.
도구마다 따로 인증할 필요가 없습니다.

## 왜 중요한가

Google · Slack · Notion 같은 커넥터는 모두 해외 서비스입니다.
국내 업무는 **일정 공유도 연락도 지도 검색도 카카오에 물려 있습니다.**
PlayMCP는 그 영역을 덮는 사실상 유일한 통로입니다.

> **요금제 확인** — 주소를 직접 넣는 커스텀 커넥터 방식은 공식 문서상 **Pro 이상**에서 됩니다
> (무료 플랜은 안 될 수 있습니다). 목록에서 바로 연결되는 경우는 그쪽을 먼저 시도해 보세요.

---

## 1단계 — 도구함에 쓸 도구를 담습니다

1. 크롬에서 아래 주소를 엽니다

   `https://playmcp.kakao.com/toolbox`

2. 오른쪽 위 **로그인** — 카카오 계정으로 로그인합니다
3. **MCP 추가하기** 를 눌러 목록에서 필요한 도구를 찾습니다
4. 도구 오른쪽 **+ 추가** 를 누릅니다. 담긴 것은 **추가됨** 으로 바뀝니다

도구는 **최대 10개**까지 담을 수 있습니다. 화면 위에 `추가한 MCP 9/10` 처럼 표시됩니다.

각 카드에서 확인할 것이 두 가지 있습니다.

| 표시 | 뜻 |
|---|---|
| `Tools 18` | 그 도구가 제공하는 기능 개수 |
| `MCP Online` / `MCP Offline` | **Offline 이면 지금 작동하지 않습니다** |

---

## 2단계 — Claude에 연결합니다

Claude로 돌아와 **커넥터** 화면을 엽니다. 검색창에 `playmcp` 를 입력하십시오.

**목록에 PlayMCP가 보이는 경우 — 이쪽이 쉽습니다**

1. **PlayMCP** 를 선택해 **연결** 을 누릅니다
2. 카카오 계정 인증 창이 뜨면 **승인**합니다

주소를 복사할 필요가 없습니다.

**목록에 안 보이는 경우**

1. PlayMCP 도구함 화면 오른쪽 위 **도구함 URL** 칸에서 **복사하기** 를 누릅니다

   주소는 누구나 같습니다 — `https://playmcp.kakao.com/mcp`

2. Claude에서 **커넥터** → 오른쪽 위 **추가** → **커스텀 커넥터** 를 선택합니다
3. 복사한 주소를 붙여넣고 연결합니다
4. 카카오 계정 인증 창이 뜨면 **승인**합니다

어느 쪽으로 연결하든 결과는 같습니다.
주소가 모두 같은데도 사람마다 다른 도구가 붙는 이유는, **카카오 계정 인증으로 본인 도구함을 찾아가기** 때문입니다.

연결되면 커넥터 목록에 **PlayMCP · 유형 웹 · 상태 ✓** 로 표시됩니다.
한 번 연결하면 Claude 웹 · 데스크톱 · 휴대폰 · Claude Code 어디서나 같은 도구함이 붙습니다.

**대화에서 붙었는지 보는 법** — 채팅창 왼쪽 아래 **`+` → `Connectors`** 를 열면
PlayMCP 가 목록에 있고, 대화별로 켜고 끌 수 있습니다.

> 도구함 화면 오른쪽에 **Claude 연결 방법** 링크가 있습니다. 화면을 보며 따라 하시려면 그것을 여십시오.

---

## 3단계 — 도구 권한을 정합니다. 여기가 제일 중요합니다

연결이 끝나면 **도구 권한** 화면이 나옵니다.
*"Claude가 이러한 도구를 사용할 수 있는 시기를 선택하세요"* 라고 적혀 있습니다.

도구는 두 묶음으로 나뉩니다. 괄호 안은 도구함에 9개를 담았을 때의 예입니다.

| 묶음 | 담기는 것 | 예 |
|---|---|---|
| **읽기 전용 도구** (47) | 조회만 하는 도구 | 카카오맵 길찾기, 네이버 검색, 주식 시세 |
| **쓰기/삭제 도구** (15) | 실행 도구 **와 분류가 애매한 조회 도구** | 카카오톡 메시지 전송, 기업 공시 조회 |

도구 오른쪽에 아이콘 세 개가 있습니다.

| 아이콘 | 뜻 |
|---|---|
| ✓ | **항상 허용** — 묻지 않고 바로 실행 |
| ✋ | **승인 필요** — 쓸 때마다 물어봄 |
| 🚫 | **차단** — 사용하지 않음 |

### 이렇게 세 번만 누르면 됩니다

**① `읽기 전용 도구 47` 묶음 → 드롭다운 → `항상 허용`**

조회만 하는 도구입니다. 카카오맵 길찾기, 네이버 검색, 주식 시세 조회가 여기 들어 있습니다.
**사고가 날 수 없는 도구들이라 항상 허용으로 두는 것이 맞습니다.**
승인 필요로 두면 47개를 쓸 때마다 창이 떠서 대화가 계속 끊깁니다.

**② `쓰기/삭제 도구 15` 묶음 → 드롭다운 → `항상 허용`**

**③ 그다음 묶음을 펼쳐 `카카오톡 나챗방: MemoChat` 하나만 `승인 필요`(✋)로 되돌립니다**

### 왜 이렇게 하나 — 묶음 이름을 그대로 믿으면 안 됩니다

`쓰기/삭제 도구` 라는 이름과 달리, 이 묶음 15개 중 **14개는 조회만 하는 도구**입니다.

| 도구 | 실제 동작 |
|---|---|
| `카카오톡 나챗방: MemoChat` | **메시지를 실제로 보냅니다** ← 이것 하나뿐 |
| `OpenDART: find_company` · `get_company_info` · `get_employees` · `get_financial_statement` 외 11개 | 기업 공시·재무제표를 **가져오기만** 합니다 |

이름이 `find_` · `get_` · `search_` 로 시작하면 **가져오는 도구**입니다.
만든 쪽이 「읽기 전용」 표시를 달지 않아 안전한 쪽으로 분류된 것뿐입니다.

그래서 묶음 전체를 승인 필요로 두면, **재무제표를 볼 때마다 승인 창이 뜹니다.**
반대로 아무 생각 없이 전체 허용하면 **카톡이 묻지 않고 나갑니다.**

위의 세 단계가 그 사이를 정확히 가릅니다 — 조회는 막힘없이, 전송만 붙잡습니다.

> **도구함 구성이 다르면 쓰기 묶음의 내용도 달라집니다.**
> ②를 하기 전에 반드시 묶음을 펼쳐 **이름을 눈으로 확인**하십시오.
> `send_` · `create_` · `delete_` · `update_` 로 시작하는 것이 보이면 그것도 승인 필요로 되돌립니다.

> 이 화면은 나중에 **커넥터 → PlayMCP** 를 눌러 언제든 다시 열 수 있습니다.
> 판단이 서지 않으면 **승인 필요**로 두십시오. 나중에 푸는 것이 먼저 열었다 되돌리는 것보다 쉽습니다.

---

## 무엇을 담을 것인가

**먼저 이 넷만 담으십시오**

| 도구 | 기능 수 | 쓰임 |
|---|---|---|
| **카카오톡 나챗방** | 1 | 결과를 나에게 카톡으로 보내기. **가장 자주 쓰게 됩니다** |
| **카카오맵** | 4 | 주소 확인, 길찾기, 주변 검색 |
| **네이버 검색 mcp** | 18 | 뉴스·블로그·카페·지역 검색 |
| **문서 요약 MCP** | 4 | 긴 문서 요약 |

이 넷은 직무를 가리지 않습니다. **첫 주에는 여기까지만** 하십시오.

**업무에 따라 추가**

| 도구 | 기능 수 | 누구에게 |
|---|---|---|
| OpenDART MCP Server | 14 | 재무·회계 — 기업 공시·재무제표 조회 |
| 한국 주식 정보 · US Stock Info | 16 · 6 | 투자·자금 업무 |
| 부동산 매물 검색 | 3 | 부동산 관련 업무 |
| 사람인 | 8 | 인사 — 채용 공고·기업 정보 |

**10개까지 담기지만 처음부터 채우지 마십시오.** 도구가 많으면 Claude가 어느 것을 쓸지 헷갈리고, 엉뚱한 도구를 꺼내 씁니다. 넷으로 두 주를 써본 뒤 필요한 것만 늘리는 편이 낫습니다.

## 도구는 나중에 바꿔도 됩니다

도구함 내용을 바꿔도 Claude 쪽 연결은 그대로 둡니다.
새 도구를 담으면 다음 대화부터 자동으로 잡힙니다. 커넥터를 다시 연결할 필요가 없습니다.

쓰지 않는 도구는 카드의 **토글을 끄면** 됩니다. 지우지 않아도 Claude에서 안 보입니다.
아예 뺄 때는 **삭제하기** 를 누릅니다.

---

## 첫 실습

Claude에 아래를 그대로 입력해 보십시오.

```
오늘 회의 내용을 세 줄로 정리해서 카톡으로 보내줘
```

나와의 채팅방에 메시지가 도착하면 연결이 정상입니다.

## 주의

PlayMCP는 **읽기와 쓰기가 모두 가능합니다.**
"이 내용 카톡으로 보내줘" 라고 하면 **실제로 메시지가 전송됩니다.** 일정도 실제로 생성됩니다.

시험 삼아 해보실 때는 반드시 **나와의 채팅방**부터 쓰십시오.
익숙해지기 전에 다른 사람이나 단체방으로 보내지 마십시오.

---

> 출처 — 도구함 최대 10개 · 게이트웨이 주소(`playmcp.kakao.com/mcp`) · 도구함 주소(`/toolbox`):
> playmcp.kakao.com/llms.txt (2026-08-30 조회).
> 커스텀 커넥터 요금제(Pro·Max·Team·Enterprise) · 대화별 켜고 끄기(`+` → Connectors):
> support.claude.com/en/articles/11175166 (2026-08-30 조회).
> 도구함·권한 화면 명칭과 묶음 구성: 2026-08-30 실화면 확인 기준이며 버전에 따라 다를 수 있습니다.
