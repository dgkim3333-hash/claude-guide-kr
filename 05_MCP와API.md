# 섹션 05 · MCP 와 API

← [04 회원가입과 커넥터](04_회원가입과커넥터.md) · [목차](../README.md) · [06 스킬](06_스킬.md) →

---

## MCP 붙이기

여기부터가 이 자료의 핵심입니다.
지금까지는 Claude에게 **말하는 법**을 가르쳤다면, 여기서는 Claude에게 **눈과 손**을 달아줍니다.
시세·법령·공시자료를 기억에 의존하지 않고 **원본에서 직접 읽어 오게** 만드는 단계입니다.

> [!IMPORTANT]
> **오늘 다 못 붙여도 괜찮습니다. 다만 건너뛰지는 마세요.**
>
> 키를 받고, 폴더를 만들고, 설정 파일을 고치는 일입니다.
> 앞 단계들처럼 붙여넣기 한 번으로 끝나지 않아 시간이 걸립니다.
> 그래도 이 단계는 **읽고 넘어가셔야 합니다.**
>
> - MCP가 무엇인지 알아야 **섹션 08 실습에서 커넥터를 왜 켜는지** 이해됩니다
> - MCP가 무엇인지 알아야 **섹션 09에서 시세가 안 나올 때 원인을 짚을 수 있습니다**
> - 붉은 오류가 떴을 때 **고장인지 준비가 덜 된 것인지** 구분할 수 있습니다
>
> **키는 나중에 넣으셔도 됩니다. 구조를 아는 것은 지금 하셔야 합니다.**

**안내문 읽는 순서** — 지침(섹션 04) → 스킬(섹션 06) → 커넥터·PlayMCP(섹션 04) → **공공데이터 API 키 → MCP 연결** (이 단계).

| 문서 | 내용 |
|---|---|
| ★ [Claude_공공데이터_API키_발급안내.md](../Claude_공공데이터_API키_발급안내.md) | **인증키 발급 전문.** 활용신청(자료마다 따로) · 승인 확인 · Encoding/Decoding · 활용기간 2년 |
| ★ [Claude_MCP_연결_안내메시지.md](../Claude_MCP_연결_안내메시지.md) | **MCP 연결 전문.** 설정 파일 붙여넣기 · datago 연결 · Firecrawl 등록 · 없는 MCP 는 Claude 에게 만들어 달라고 |
| ★ [MCP_동작원리.md](../MCP_동작원리.md) | **붙이기 전에 한 번 읽어보세요.** 설정 파일에 적으면 무슨 일이 벌어지나 · 다섯 관문 · 빨간불과 빈 답의 차이 |
| ★ 이 문서 맨 아래 「기본 config 에 들어 있는 MCP 여덟 개」 절 | **8개가 각각 무엇을 해주나.** 서버별 쓰임새 · 이렇게 말하면 됩니다 · 여기 없는 것들 |
| ★ [중요_claude_desktop_config_기본.json](../중요_claude_desktop_config_기본.json) | **MCP 8개 — 키 없이 되는 7개 + API 예제 1개(firecrawl).** 여기부터 시작하세요 |
| [claude_desktop_config_실습용.jsonc](../claude_desktop_config_실습용.jsonc) | 설명이 잔뜩 달린 확장판. 키 발급처와 주의사항 포함 |

## 설정 파일 여는 법 — 설정 → 데스크톱 앱 → 개발자 → [구성 편집]

> 📄 교재 PDF **p.47~48** — 설정 파일은 버튼으로 엽니다 · 붙여넣고 캡처해서 확인받으세요

**숨은 폴더 경로를 손으로 찾아갈 필요가 없습니다.** 버튼이 대신 열어줍니다.

| 순서 | 할 일 |
|---|---|
| 1 | 설정 → 왼쪽 목록 아래쪽 **`데스크톱 앱`** → **`개발자`** |
| 2 | 「로컬 MCP 서버」의 **[구성 편집]** 을 누릅니다 |
| 3 | 파일 탐색기가 열리고 **`claude_desktop_config`** 파일이 보입니다 |
| 4 | 그 파일을 **더블클릭** — 메모장이나 VS Code로 열립니다 |
| 5 | 내용을 바꾸고 **저장**합니다 (인코딩 UTF-8) |
| 6 | **Claude 데스크탑을 완전히 종료했다 다시 켭니다** → 아래 「완전 종료」 를 그대로 따라 하세요 |

**처음 쓰시는 분은** 받은 `중요_claude_desktop_config_기본.json` 을 더블클릭으로 열고,
**내용을 전부 복사해 내 `claude_desktop_config.json` 에 붙여넣는 것이 편합니다.**
받은 파일에서 `Ctrl+A` → `Ctrl+C`, 내 파일 탭으로 옮겨 `Ctrl+A` → `Ctrl+V` → `Ctrl+S` — 이게 전부입니다.
화면 순서는 [Claude_MCP_연결_안내메시지.md](../Claude_MCP_연결_안내메시지.md) 에 그대로 있습니다.

> ★ **고치기 전에 원본을 복사해 두세요.** 콤마 하나만 어긋나도 MCP가 하나도 안 뜹니다.
> ★ `claude_desktop_config_실습용.jsonc` 는 **그대로 넣으면 안 됩니다.** 주석(`//`)이 들어 있어
>   앱이 못 읽을 수 있습니다. 그건 읽고 이해하는 용도이고, 실제로 넣는 것은 주석 없는 `.json` 쪽입니다.

### MCP가 안 붙을 때도 이 화면에서 봅니다

같은 `개발자` 화면 왼쪽에 **붙어 있는 서버 목록**이 나옵니다. 하나를 고르면

- **`running`** 인지 아닌지
- 실행에 쓰는 **명령어**와 **인수**(경로가 맞는지 여기서 확인)
- **[로그 보기]** — 왜 실패했는지 이 로그에 적혀 있습니다

붉은 오류가 났을 때 "왜 안 되지" 하고 헤매지 마시고 **로그부터 여세요.**

## 「Claude 를 완전히 종료한다」가 무슨 뜻인가 — 완전 종료

> 📄 교재 PDF **p.18 · p.49** — X 를 눌러도 안 꺼집니다 · 저장·종료·재실행·새 대화

**창 오른쪽 위 X 를 눌러도 Claude 는 꺼지지 않습니다.**
화면에서만 사라지고 **뒤에서 계속 돌아갑니다.** 이 상태로 다시 열면
설정을 아무리 고쳐도 반영되지 않습니다. **여기서 가장 많이 막힙니다.**

| 순서 | 할 일 |
|---|---|
| 1 | 창의 **X** 를 눌러 창을 닫습니다 |
| 2 | 화면 **오른쪽 아래, 시계 왼쪽**을 보세요. **`∧`** (위를 향한 작은 화살표) 가 있습니다 |
| 3 | **`∧`** 를 클릭하면 아이콘이 여러 개 든 **작은 상자**가 뜹니다 |
| 4 | 그 안에서 **Claude 아이콘**을 찾습니다 — **검은 별표(✳) 모양**입니다. 마우스를 올리면 이름이 떠서 확인됩니다 |
| 5 | 그 아이콘에 **마우스 오른쪽 버튼** → **종료**(Quit) |
| 6 | **아이콘이 상자에서 사라지면** 완전히 꺼진 것입니다 |
| 7 | 시작 메뉴에서 Claude 를 다시 실행합니다 |

> **상자에 Claude 아이콘이 아예 없으면** 이미 꺼져 있는 것입니다. 그냥 다시 실행하세요.
> **`∧` 가 안 보이면** 아이콘이 이미 펼쳐져 있는 것입니다. 작업 표시줄 오른쪽을 그대로 살펴보세요.
> 사람마다 상자 안 아이콘 개수와 배치가 다릅니다. 개수로 찾지 마시고 **모양으로** 찾으세요.

**컴퓨터를 다시 시작하는 것과는 다른 이야기입니다.** Claude 만 껐다 켜면 됩니다.

트레이 상자와 Claude 아이콘(✳)이 어떻게 생겼는지는 **교재 p.18** 에 실제 화면이 있습니다.

## 기본 config 안에는 두 종류가 들어 있습니다

> 📄 교재 PDF **p.24 · p.29** — Node.js 와 uv · 설치 자료는 GitHub 에

| 구분 | 서버 | 준비물 |
|---|---|---|
| **바로 되는 것 (7개)** | memory · youtube-transcript · context7 · sequential-thinking · playwright · hwp-read · word | **없음.** 붙여넣고 재시작하면 끝 |
| **API 키 예제 (1개)** | **firecrawl** | firecrawl.dev 무료 가입 후 키 하나. **폴더 불필요** |

**키가 없는 상태로 붙여넣으면 firecrawl 하나만 붉은 오류가 납니다.** 고장이 아닙니다.
나머지 7개는 정상 작동합니다. firecrawl 은 「API 키를 넣는 법」을 배우는 **예제**로 하나만 남긴 것입니다.
키 받는 법과 쓰는 법은 아래 **「Firecrawl — 수업에서 같이 붙입니다」** 절에 있습니다.

> [!NOTE]
> **★ datagokr 는 기본 config 에서 뺐습니다 (2026-09-03)**
>
> 공공데이터포털 서버는 **소스 폴더를 따로 받아 `uv sync` 까지 해야** 움직입니다.
> 수업 중에 20명이 동시에 하기에는 막히는 자리가 너무 많아, **수업이 끝난 뒤 각자 하시도록** 옮겼습니다.
> **자료는 그대로 다 있습니다.** 아래 절을 보고 천천히 하시면 됩니다.
> 경매 실습(섹션 09)을 하실 분은 [경매물건분석/config_경매용.json](../경매물건분석/config_경매용.json) 에 datagokr 가 그대로 들어 있습니다.

### datagokr — 수업이 끝난 뒤에 천천히 하세요

먼저 솔직히 말씀드립니다. **이 하나가 이 교재에서 가장 어렵습니다.**
다른 MCP 는 설정 파일에 몇 줄 붙여넣으면 끝나는데, 이것만 **프로그램을 따로 받아서 깔아야** 합니다.

처음 하시면 **1시간 남짓** 걸립니다. 활용신청에만 20~30분이 듭니다. 조용한 시간에 앉아서 하십시오.
**막히면 그 화면을 캡처해서 Claude 에게 물어보시면 됩니다.** 혼자 붙들고 계시지 마세요.

> **시작하기 전에 [MCP 는 어떻게 도는가](../MCP_동작원리.md) 를 한 번 읽어보십시오.**
> 왜 폴더가 필요하고 왜 `uv sync` 를 해야 하는지 알고 하시면 훨씬 덜 헤맵니다.

#### ★★ 이것 하나만 알면 MCP 전체가 이해됩니다 — 경로와 파일

**설정 파일에 넣는 몇 줄은 명령이 아니라 「주소」입니다.**
Claude 는 그 주소로 찾아가서 프로그램을 실행합니다.
**주소가 가리키는 자리에 진짜 파일이 없으면 아무 일도 일어나지 않습니다.**

MCP 를 붙이는 길이 두 가지인 이유가 바로 여기 있습니다.

| | 기본 config 의 일곱 개 | **datagokr** |
|---|---|---|
| 설정에 적는 것 | `npx -y 패키지이름` | `uv --directory C:\AI\mcp\datagokr-mcp` |
| 그 줄의 뜻 | 「인터넷에서 받아서 실행해」 | 「**내 컴퓨터 이 폴더**에 있는 걸 실행해」 |
| 내 컴퓨터에 필요한 것 | **없습니다** | **폴더와 파일이 반드시 있어야 합니다** |
| 그래서 순서가 | 붙여넣고 → 재시작. 끝 | 만들고 → 부품 받고 → **그다음에** 등록 |

> [!IMPORTANT]
> **★ 「설정 파일에 넣었는데 왜 안 되죠?」의 답이 이것입니다**
>
> 주소만 적어두고 그 자리에 파일을 안 만들어 둔 것입니다.
> **주소를 적는 일과 파일을 만드는 일은 별개입니다.**
> 이사 갈 집 주소를 수첩에 적었다고 집이 지어지는 것은 아닙니다.

**1단계를 끝내면 이런 폴더가 생깁니다.** 직접 열어서 눈으로 확인하십시오.

```
C:\AI\mcp\datagokr-mcp\
 ├ server.py          ← 프로그램 본체. 이 파일이 없으면 아무것도 안 됩니다
 ├ pyproject.toml     ← 어떤 부품이 필요한지 적어둔 목록
 ├ 실행순서.md         ← Claude 가 남겨준 안내문. 막히면 이것부터 보십시오
 ├ .venv\             ← 3단계에서 uv sync 를 하면 생깁니다 (부품 보관소)
 └ __pycache__\       ← 실행하면 저절로 생깁니다. 지우셔도 됩니다
```

**이 폴더가 곧 설정 파일에 적을 주소입니다.** 둘이 한 글자라도 다르면 안 켜집니다.

**무엇이 없으면 어떤 증상이 나는가**

| 빠진 것 | 증상 | 어디를 고치나 |
|---|---|---|
| 폴더 자체가 없다 | **빨간불** | 1단계를 안 한 것입니다 |
| 경로에 오타가 있다 | **빨간불** | 설정 파일의 `--directory` 뒤를 탐색기 주소와 대조 |
| `server.py` 가 없다 | **빨간불** | Claude 가 파일을 못 만든 것 — 폴더 연결을 확인 |
| `uv sync` 를 안 했다 | **빨간불** | 3단계로 돌아가십시오 |
| 인증키를 안 넣었다 | 빨간불 없음 · **빈 답** | 4단계 `env` 안의 키 |
| 활용신청을 안 했다 | 빨간불 없음 · **빈 답** | 0단계로 돌아가십시오 |

> **빨간불과 빈 답은 원인이 다릅니다.**
> 빨간불은 「서버가 아예 안 떴다」이고, 빈 답은 「떴는데 자료를 못 받았다」입니다.
> 자세한 것은 [MCP 는 어떻게 도는가](../MCP_동작원리.md) 에 있습니다.

**★ datagokr 하나만 제대로 붙여보시면 나머지 MCP 는 훨씬 쉬워집니다.**
「폴더가 필요한 것」과 「필요 없는 것」의 차이를 몸으로 알게 되기 때문입니다.

---

**전체 순서는 이렇습니다.**

```
0단계  활용신청       공공데이터포털에서 쓸 자료를 신청 ← 먼저 걸어두세요
1단계  만들기       Cowork 에서 Claude 에게 만들어 달라고 하기
2단계  PowerShell     그 폴더에서 명령어 창 열기
3단계  uv sync        부품 받기 (2~5분)
4단계  설정 파일      config 에 등록 + 키 넣기
5단계  재시작         Claude 완전 종료 후 다시 켜기
6단계  확인           진짜로 자료가 오는지
```

---

#### 0단계 — 활용신청부터 걸어두세요

**이걸 안 하면 설치를 다 끝내고도 빈 답을 받습니다.** 빨간불도 안 뜨니 원인을 못 찾습니다.

공공데이터포털은 **자료 하나하나마다 따로 신청**해야 열립니다.
승인에 시간이 걸릴 수 있으니 **가장 먼저 신청해두고**, 그동안 1~3단계를 진행하십시오.

```
https://www.data.go.kr
```

가입 → 검색창에 아래 이름을 넣고 → **`활용신청`** 버튼

> [!WARNING]
> **★ 한 번에 여러 개를 신청할 수 없습니다**
>
> **API 하나마다 따로 신청해야 합니다.** 목록에서 여러 개를 골라 한꺼번에 넣는 기능은 없습니다.
> **8건이면 「검색 → 활용신청 → 목적 입력 → 신청」을 8번** 반복하셔야 합니다.
> 건당 2~3분, **전부 하면 20~30분** 걸립니다. 각오하고 한자리에 앉아 몰아서 하십시오.
>
> **다만 그 API 안의 「상세기능」은 한 번에 전부 체크됩니다.**
> 예를 들어 「아파트 매매 실거래가」 하나를 신청하면서 상세기능을 전부 켜두면 그 안은 다 열립니다.
> **체크는 공짜입니다. 빠뜨리면 나중에 그 API 를 다시 신청해야 합니다.**
>
> **시간을 줄이는 요령** — 「활용목적」에 쓸 문장을 **메모장에 미리 한 줄 적어두고 복사·붙여넣기** 하십시오.
> 매번 새로 쓰면 그게 제일 오래 걸립니다. 예: `개인 학습 및 부동산 자료 조회 목적`

**★ 경매·부동산 실습에 꼭 필요한 것은 8건입니다.**

| # | 신청할 서비스 이름 | 바로 검색 |
|---|---|---|
| 1 | **행정안전부_행정표준코드 법정동코드** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EB%B2%95%EC%A0%95%EB%8F%99%EC%BD%94%EB%93%9C&dType=API) |
| 2 | **국토교통부_아파트 매매 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EC%95%84%ED%8C%8C%ED%8A%B8%20%EB%A7%A4%EB%A7%A4%20%EC%8B%A4%EA%B1%B0%EB%9E%98%EA%B0%80&dType=API) |
| 3 | **국토교통부_아파트 전월세 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EC%95%84%ED%8C%8C%ED%8A%B8%20%EC%A0%84%EC%9B%94%EC%84%B8&dType=API) |
| 4 | **국토교통부_연립다세대 매매 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EC%97%B0%EB%A6%BD%EB%8B%A4%EC%84%B8%EB%8C%80%20%EB%A7%A4%EB%A7%A4%20%EC%8B%A4%EA%B1%B0%EB%9E%98%EA%B0%80&dType=API) |
| 5 | **국토교통부_연립다세대 전월세 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EC%97%B0%EB%A6%BD%EB%8B%A4%EC%84%B8%EB%8C%80%20%EC%A0%84%EC%9B%94%EC%84%B8&dType=API) |
| 6 | **국토교통부_단독/다가구 매매 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EB%8B%A8%EB%8F%85%20%EB%8B%A4%EA%B0%80%EA%B5%AC%20%EB%A7%A4%EB%A7%A4%20%EC%8B%A4%EA%B1%B0%EB%9E%98%EA%B0%80&dType=API) |
| 7 | **국토교통부_단독/다가구 전월세 실거래가 자료** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EB%8B%A8%EB%8F%85%20%EB%8B%A4%EA%B0%80%EA%B5%AC%20%EC%A0%84%EC%9B%94%EC%84%B8&dType=API) |
| 8 | **국토교통부_건축HUB_건축물대장정보 서비스** | [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EA%B1%B4%EC%B6%95HUB%20%EA%B1%B4%EC%B6%95%EB%AC%BC%EB%8C%80%EC%9E%A5&dType=API) |

**「열기」를 누르면 오픈 API 만 걸러진 목록이 바로 뜹니다.** 검색어를 직접 치실 필요 없습니다.

**한 건마다 이 네 단계입니다.**

1. 왼쪽 필터에서 **`오픈 API`** 를 눌러 파일데이터를 걸러냅니다
2. 목록에서 이름을 확인하고 **오른쪽 `활용신청`** 버튼을 누릅니다 — **상세 페이지에 들어가지 않아도 됩니다**
3. **활용목적** 한 줄 + **상세기능 전부 체크** + **라이선스 동의**
4. **신청** — 대부분 즉시 승인됩니다

> **⚠️ 이름이 비슷한 것이 나란히 뜹니다. 헷갈리기 쉽습니다.**
>
> 「아파트 매매 실거래가」로 검색하면 이 셋이 함께 나옵니다.
>
> | 이름 | 신청? |
> |---|---|
> | 국토교통부_아파트 매매 실거래가 **자료** | **← 이것입니다** |
> | 국토교통부_아파트 매매 실거래가 **상세** 자료 | 아닙니다 |
> | 국토교통부_아파트 **분양권전매** 실거래가 자료 | 아닙니다 |
>
> **「상세」도 「분양권전매」도 아닌, 이름이 가장 짧은 것**을 고르십시오.
> (data.go.kr 검색 결과 직접 확인 · 2026-09-03)

> **★ 이 8건이면 `real-estate` 도 같이 열립니다.** 두 MCP 가 같은 자료를 쓰기 때문에
> **한 번만 신청하면 둘 다 됩니다.** 따로 하실 필요 없습니다.

**나머지는 필요해질 때 추가하십시오.**

| 나중에 | 건수 | 언제 |
|---|---|---|
| 캠코 온비드 (공매) | **19건** | 공매를 하실 분만 — [열기](https://www.data.go.kr/tcs/dss/selectDataSetList.do?keyword=%EC%98%A8%EB%B9%84%EB%93%9C&dType=API) |
| 오피스텔 매매·전월세 | 2건 | 오피스텔을 보실 때 |
| 토지·상업업무용·공장창고 | 3건 | 해당 물건을 보실 때 |
| 소상공인 상권정보 · 공동주택 단지 · 도시철도 | 4건 | 상권·단지 정보가 필요할 때 |

**전부 신청하면 38건입니다. 처음부터 다 하지 마십시오.**

> **★ 이렇게 되면 성공** — **마이페이지 → Open API → 활용신청 현황**에서
> 신청한 것들이 **「승인」**으로 보입니다. 대부분 즉시 승인됩니다.

**인증키도 여기서 받습니다.** 마이페이지 → Open API → **인증키 발급현황**.
**Encoding 이 아니라 Decoding 쪽**을 복사하십시오. 두 개가 나란히 있어서 가장 많이 틀리는 자리입니다.
자세한 화면은 [Claude_공공데이터_API키_발급안내.md](../Claude_공공데이터_API키_발급안내.md) 에 있습니다.

---

#### 1단계 — Claude 에게 만들어 달라고 하기

**남이 만든 것을 받아오는 게 아니라, Claude 가 그 자리에서 만들어 줍니다.**
교재 섹션 05의 「없는 MCP 는 Claude 에게 만들어 달라고 하세요」를 실제로 해보는 자리입니다.

> [!IMPORTANT]
> **★★ 이건 Cowork 에서만 됩니다**
>
> **일반 대화창에서는 Claude 가 코드를 「글로만」 써줍니다.** 파일도 폴더도 만들지 못합니다.
> 화면에 나온 코드를 직접 복사해 파일로 저장하셔야 합니다.
>
> **Cowork 는 폴더를 연결해두면 파일을 직접 만들어 줍니다.** 이 방법을 쓰시려면 Cowork 를 여십시오.
> Cowork 가 무엇인지는 [섹션 08 「Cowork 와 Code」](08_실전_테마레이더.md) 를 보십시오.

> [!IMPORTANT]
> **★ 활용신청과 도구 만들기는 다른 일입니다**
>
> **활용신청**은 「그 자료를 받을 **권한**」이고, **코드**는 「그 자료를 **가져오는 프로그램**」입니다.
> 활용신청을 100건 해두어도 **코드에 그 API 가 없으면 못 씁니다.**
> 그래서 아래 ③에서 **무엇을 만들지 말해주셔야** 합니다.
>
> 반대도 마찬가지입니다. 코드에 있어도 활용신청이 없으면 **빈 답**이 옵니다.
> **둘 다 있어야 합니다.**

**① `C:\AI\mcp` 폴더를 만듭니다**

탐색기 주소창에 `C:\` 를 치고 들어가 `AI` 폴더를 만들고, 그 안에 `mcp` 폴더를 만드십시오.
**이미 있으면 새로 만들지 마시고 그대로 쓰십시오.**

**② Cowork 에서 그 폴더를 연결합니다**

Cowork 를 열고 **폴더 연결**에서 `C:\AI\mcp` 를 고르십시오.
**연결하지 않으면 Claude 가 그 폴더에 파일을 못 만듭니다.**

**③ 무엇을 만들지 정하고 말합니다**

기본 틀은 이렇습니다.

```
공공데이터포털 자료를 Claude 가 직접 가져오게 해줘.
폴더는 C:\AI\mcp\datagokr-mcp 로 만들어줘.

아래를 되게 해줘.
(여기에 아래 표에서 고른 것을 적으십시오)

파이썬으로 만들고, 설정 파일에 넣을 블록도 같이 줘.
API 주소가 맞는지 실제로 호출해서 확인해줘.
```

**관심사에 맞춰 고르십시오.** 세 벌을 준비해두었습니다.

<details open>
<summary><b>■ 경매·부동산을 보시는 분</b></summary>

```
- 행정안전부 법정동코드 조회
- 국토교통부 아파트 매매 실거래가
- 국토교통부 아파트 전월세
- 국토교통부 연립다세대 매매·전월세
- 국토교통부 단독다가구 매매·전월세
- 건축HUB 건축물대장 (표제부·전유부·층별·총괄)
```

활용신청 **8건**이면 다 열립니다. **`real-estate` MCP 도 같은 자료를 쓰므로 함께 열립니다.**

</details>

<details>
<summary><b>■ 공매(온비드)에 관심 있으신 분</b></summary>

```
- 행정안전부 법정동코드 조회
- 캠코 온비드 물건 목록 조회
- 캠코 온비드 물건 상세 조회
- 캠코 온비드 입찰결과 목록·상세
- 캠코 온비드 공고 목록·상세
- 국토교통부 아파트 매매 실거래가   ← 낙찰가와 시세를 대조하려면 필요합니다
```

> **★ 온비드는 함정이 하나 있습니다. 미리 알려주십시오.**
>
> ```
> 온비드 API 의 기관코드는 B010003 이야. 1360000 은 기상청이니까 쓰지 마.
> ```
>
> 이 한 줄을 같이 넣으면 실패를 크게 줄일 수 있습니다.
> **실제로 이 자리에서 한 번 틀렸던 기록이 있습니다.**

활용신청은 온비드 쪽만 **여러 건**입니다. 목록·상세·입찰결과·공고가 각각 따로입니다.
**전부 하지 마시고 위에 적은 것부터** 신청하십시오.

</details>

<details>
<summary><b>■ 기업·상권 정보를 보시는 분</b></summary>

```
- 행정안전부 법정동코드 조회
- 소상공인 상권정보 (반경 내 상가 조회)
- 국토교통부 상업업무용 부동산 매매 실거래가
```

</details>

> **★ 한 번에 스무 개씩 시키지 마십시오.**
> 많이 시킬수록 오래 걸리고, 어디서 틀렸는지 찾기 어려워집니다.
> **5~8개로 시작해 되는 것을 확인한 뒤 늘리는 편이 훨씬 빠릅니다.**
>
> 나중에 「여기에 토지 실거래가도 추가해줘」라고 하시면 그 자리에 붙여 줍니다.
> **한 번 만들어두면 늘리는 건 쉽습니다.**

**④ Claude 가 만드는 것을 지켜보십시오**

API 주소가 맞는지 직접 호출해보고 고치기도 합니다. **몇 분 걸립니다.**

> **★ 이렇게 되면 성공** — `C:\AI\mcp\datagokr-mcp` 에 이런 파일들이 생깁니다.
>
> ```
> C:\AI\mcp\datagokr-mcp\
>  ├ server.py          ← 프로그램 본체
>  └ pyproject.toml     ← 필요한 부품 목록
> ```
>
> **폴더 이름을 바꾸지 마세요.** 설정 파일이 `datagokr-mcp` 를 글자 그대로 찾습니다.

**⑤ 무엇이 만들어졌는지 확인하십시오**

**사람마다 결과가 다릅니다.** 도구 개수도 이름도 그때그때 달라집니다.
Claude 에게 이렇게 물어보십시오.

```
방금 만든 것에 어떤 도구가 들어 있는지, 각각 어떤 활용신청이 필요한지 목록으로 알려줘
```

**이 목록이 0단계에서 신청할 것과 맞아야 합니다.** 여기서 빠진 게 있으면 지금 추가하십시오.

> **★ 이미 만들어진 것을 받고 싶으시면** — 이 저장소에도 예제(47개)가 들어 있습니다.
> [경매물건분석/mcp/datagokr-mcp](https://github.com/dgkim3333-hash/claude-guide-kr/tree/main/%EA%B2%BD%EB%A7%A4%EB%AC%BC%EA%B1%B4%EB%B6%84%EC%84%9D/mcp/datagokr-mcp)
> 를 통째로 복사해 `C:\AI\mcp\` 에 넣으시면 ①~⑤ 를 건너뛸 수 있습니다.
> 다만 **직접 만들어보시길 권합니다.** 이 과정을 한 번 겪으면 MCP 가 무엇인지 몸으로 알게 됩니다.

---

#### 2단계 — PowerShell 열기

가장 쉬운 방법입니다.

**① 탐색기로 `C:\AI\mcp\datagokr-mcp` 폴더를 엽니다**
**② 위쪽 주소창을 한 번 클릭** — 주소가 파랗게 선택됩니다
**③ 거기에 `powershell` 이라고 치고 `Enter`**

> **★ 이렇게 되면 성공** — 파란 창이 열리고 맨 아랫줄이 이렇게 되어 있습니다.
>
> ```
> PS C:\AI\mcp\datagokr-mcp>
> ```
>
> **이 방법으로 열면 이미 그 폴더 안에 들어와 있는 상태**라 바로 3단계로 가시면 됩니다.

시작 메뉴에서 여셨다면 `PS C:\Users\내이름>` 처럼 나옵니다. 그때는 이 줄을 먼저 넣으십시오.

```
cd C:\AI\mcp\datagokr-mcp
```

---

#### 3단계 — `uv sync` 로 부품 받기

```
uv sync
```

**이 명령이 무엇을 하는지 알아두시면 덜 불안합니다.**
이 프로그램은 남이 만든 파이썬 부품 네 개를 씁니다. 그 부품들은 폴더 안에 없어서 인터넷에서 받아야 합니다.
`uv sync` 가 `pyproject.toml`(부품 주문서)을 읽고 받아다 **`.venv`(부품 창고)** 를 만들어 줍니다.

**처음 한 번은 2~5분 걸립니다.** 화면에 아무 변화가 없어 보여도 **멈춘 것이 아닙니다.**
창을 닫지 마시고 기다리십시오.

> **★ 이렇게 되면 성공** — 이런 줄들이 지나가고 다시 `PS ...>` 로 돌아옵니다.
>
> ```
> Resolved 30 packages in 1.2s
> Installed 28 packages in 3.4s
> ```
>
> 그리고 폴더 안에 **`.venv` 폴더가 새로 생겨 있습니다.** 이게 생겼으면 제대로 된 것입니다.
>
> 4개를 주문했는데 28개가 깔립니다. **부품이 또 다른 부품을 부르기 때문이며 정상입니다.**

**「uv 를 인식할 수 없습니다」** 가 나오면 uv 가 아직 없는 것입니다.
[사전준비_설치.md](../사전준비_설치.md) 로 가서 uv 를 깔고, **PowerShell 을 닫았다가 새로 열어** 다시 하십시오.

---

#### 4단계 — 설정 파일에 등록하고 키 넣기

`설정 → 개발자 → 구성 편집` 으로 파일을 열고, `"mcpServers": {` **바로 아래**에 넣습니다.

```json
"datagokr": {
  "command": "uv",
  "args": ["--directory", "C:\\AI\\mcp\\datagokr-mcp", "run", "python", "server.py"],
  "env": {
    "DATA_GO_KR_SERVICE_KEY": "여기에_Decoding_인증키",
    "EPOST_SERVICE_KEY": "여기에_Decoding_인증키",
    "PYTHONIOENCODING": "utf-8",
    "PYTHONUTF8": "1"
  }
},
```

- **0단계에서 받은 Decoding 키**를 두 곳에 같이 넣으시면 됩니다
- `EPOST_SERVICE_KEY` 는 우편번호·도로명주소 조회에 쓰입니다
- **블록 끝 쉼표(`},`)** 를 빠뜨리지 마십시오

> **★ 이렇게 되면 성공** — 붙여넣은 화면을 캡처해서 Claude 에게
> **「제대로 붙였는지 확인해줘」** 라고 물으면 쉼표·중괄호까지 봐줍니다.
> 캡처할 때 **다른 MCP 의 `env` 부분은 접어서** 다른 키가 같이 찍히지 않게 하십시오.

---

#### 5단계 — Claude 완전 종료 후 재시작

**설정 파일은 켤 때만 읽습니다.** 그래서 반드시 껐다 켜야 합니다.

**창의 `X` 로는 안 꺼집니다.** 시계 옆 트레이 아이콘에서 오른쪽 클릭 → 종료까지 하십시오.
자세한 방법은 이 문서 앞쪽 「완전 종료」 절에 있습니다.

---

#### 6단계 — 진짜로 되는지 확인

**새 대화**를 열고 이렇게 물어보십시오.

```
오산시 원동 법정동코드 알려줘
```

> **★ 이렇게 되면 성공** — `4137010300` 이라는 열 자리 숫자가 나옵니다.

이어서 하나 더 해보십시오.

```
시군구코드 41370, 2026년 8월 아파트 매매 실거래가 보여줘
```

**★ 이 확인을 꼭 하십시오.** 설정 화면의 서버 목록에 `datagokr` 이름이 떠 있는 것과
실제로 자료가 오는 것은 **다른 이야기입니다.** 이름만 떠 있고 안 되는 경우가 가장 흔합니다.

---

#### 막혔을 때 — 이 순서로 봐주세요

| 증상 | 어느 단계 | 어떻게 |
|---|---|---|
| 설정 화면에 `datagokr` 이 아예 없다 | 4단계 | 쉼표·중괄호 오류. 캡처해서 Claude 에게 확인 요청 |
| `uv 를 인식할 수 없습니다` | 3단계 | PowerShell 을 닫고 새로 연 뒤 다시. 그래도 안 되면 uv 재설치 |
| `경로를 찾을 수 없습니다` | 2단계 | `C:\AI\mcp\datagokr-mcp` 에 `server.py` 가 보이는지 확인 |
| 폴더 안에 폴더가 또 있다 | 1단계 | 안쪽 `datagokr-mcp` 를 꺼내서 `C:\AI\mcp\` 바로 아래로 |
| `uv sync` 가 한참 멈춰 있다 | 3단계 | 정상입니다. 2~5분 기다리세요 |
| 이름은 뜨는데 빨간불 | 3단계 | `.venv` 폴더가 생겼는지 확인 → 없으면 `uv sync` 다시 |
| **이름은 뜨는데 답이 안 온다** | 4단계 | **Encoding 키를 넣은 것입니다.** Decoding 으로 바꾸십시오 |
| **답은 오는데 내용이 비어 있다** | **0단계** | **그 자료의 활용신청을 안 했거나 승인 대기 중입니다** |
| 잘 되던 게 어느 날 멈췄다 | 0단계 | 키 활용기간(2년) 만료. 「활용연장」 신청 |

**그래도 안 되면 오류 화면을 그대로 캡처해서 Claude 에게 보여주십시오.**
「MCP 가 안 붙어요」라고만 말해도 [MCP 점검 스킬](../mcp-health-check.skill)이 설정 파일과 로그를 읽어 짚어줍니다.

---

#### 붙이면 무엇이 되나

| 자료 | 도구 수 | 상태 |
|---|---|---|
| 건축HUB 건축물대장 (총괄·표제부·전유부·층별) | 9 | 확인 완료 |
| 국토교통부 실거래가 | 12 | 확인 완료 |
| 행안부 법정동코드 · 우정사업본부 도로명주소 | 2 | 확인 완료 |
| 소상공인 상권정보 | 2 | 확인 완료 |
| 캠코 온비드 (공매) | 19 | **일부만 확인** — 활용신청 상태에 따라 다릅니다 |
| 공동주택 단지 · 도시철도 노선 | 3 | **확인 중** |

**온비드 쪽은 안 나올 수 있습니다. 고장이 아닙니다.**
더 자세한 내용은 [경매물건분석/필요한MCP.md](../경매물건분석/필요한MCP.md) 3순위 절과
소스 폴더 안의 `README.md` 에 있습니다.

> [!NOTE]
> **★ 예전 배포본(13개)을 받으셨던 분**
>
> 키가 필요한 서버 5개(firecrawl · korean-law · real-estate · vworld-landuse · kis)는
> **연결 오류가 너무 자주 나서 기본 config 에서 뺐습니다** (2026-08-30).
> 없어진 게 아니라 자리를 옮긴 것입니다 —
> **경매용 4개**(korean-law · real-estate · vworld-landuse · datagokr)는 [경매물건분석/config_경매용.json](../경매물건분석/config_경매용.json) 에,
> **주식용 kis** 는 바로 아래 절에 있습니다. firecrawl 은 배포에서 제외했습니다.
> 예전 13개 그대로 쓰셔도 고장은 아니지만, 키를 안 넣은 서버마다 붉은 오류가 계속 뜹니다.

> [!WARNING]
> **★★ 이미 config 를 넣으신 분 — `youtube` 가 안 될 겁니다 (2026-09-03 교체)**
>
> 예전 배포본에는 **`youtube`** 서버가 들어 있었습니다. **그건 `yt-dlp` 라는 별도 프로그램이
> 따로 깔려 있어야 움직입니다.** 안 깔려 있으면 붉게 뜨거나, 떠 있어도 답이 안 옵니다.
> **고장이 아니라 준비물이 하나 빠진 것**입니다.
>
> 「키 없이 바로 되는 것」이라고 안내해놓고 실제로는 외부 프로그램이 필요했습니다. 제 잘못입니다.
> **그래서 `youtube` 를 빼고 `memory` 로 바꿨습니다.**
>
> **두 가지 중 하나를 고르십시오.**
>
> **① config 를 바꾼다 (권장)** — 설정 파일에서 `"youtube": { ... },` 블록을 통째로 지우고
> 그 자리에 아래를 넣으십시오. **키가 필요 없고 바로 됩니다.**
>
> ```json
> "memory": {
>   "command": "npx",
>   "args": ["-y", "@modelcontextprotocol/server-memory"]
> },
> ```
>
> `memory` 는 **대화가 바뀌어도 기억하는** 서버입니다. 「내 관심종목은 ○○」, 「나는 오산 물건을 본다」를
> 한 번 알려두면 다음 대화에서도 압니다. **섹션 04 개인설정이 「고정으로 적는 것」이라면 이건 「쓰면서 쌓이는 것」**입니다.
>
> **② `yt-dlp` 를 깐다** — 유튜브 **영상 파일 다운로드**가 꼭 필요하시면 이쪽입니다.
> PowerShell 에 한 줄 넣으십시오.
>
> ```
> winget install yt-dlp
> ```
>
> 깐 뒤 **PowerShell 을 닫고 새로 열어** `yt-dlp --version` 으로 확인하고,
> **Claude 를 완전히 종료했다가 다시 켜십시오.**
>
> **어느 쪽이든 유튜브 자막·요약은 그대로 됩니다.** `youtube-transcript` 가 따로 있고,
> 그건 `yt-dlp` 없이 혼자 동작합니다. ②를 안 하셔도 영상 내용 요약은 됩니다.

> [!NOTE]
> **`kis` 는 기본 config 에 없습니다 — 주식 실습(섹션 08) 하실 때만 추가**
>
> 한국투자증권 시세를 가져오는 서버입니다. 기본 config 에서 뺐으므로,
> 섹션 08 주식 실습을 하실 때 아래 블록을 `"firecrawl": { ... }` 뒤에 **콤마를 붙이고** 이어 넣으세요.
>
> ```json
> "kis": {
>   "command": "uv",
>   "args": [
>     "run",
>     "--directory",
>     "C:\\AI\\mcp\\kis-mcp-server",
>     "python",
>     "server.py"
>   ],
>   "env": {
>     "KIS_APP_KEY": "<<여기에_KIS_APP_KEY_입력>>",
>     "KIS_APP_SECRET": "<<여기에_KIS_APP_SECRET_입력>>",
>     "KIS_CANO": "<<여기에_계좌번호_앞8자리_입력>>",
>     "KIS_ACNT_PRDT_CD": "01",
>     "KIS_ACCOUNT_TYPE": "VIRTUAL",
>     "KIS_MCP_TOOLSET": "catalog",
>     "KIS_MCP_LOG_LEVEL": "WARNING",
>     "PYTHONIOENCODING": "utf-8",
>     "PYTHONUTF8": "1"
>   }
> }
> ```
>
> 준비물이 셋입니다.
>
> | 무엇 | 어디서 |
> |---|---|
> | KIS MCP 소스 | 내려받아 `C:\AI\mcp\kis-mcp-server` 에 둡니다. 다른 폴더면 `--directory` 값을 고치세요 |
> | 앱키 · 시크릿 | `apiportal.koreainvestment.com` — **승인에 1~2일** 걸립니다 |
> | 계좌번호 앞 8자리 | `KIS_CANO` 칸 |
>
> **`KIS_ACCOUNT_TYPE` 이 `VIRTUAL`(모의투자)로 되어 있습니다.** 처음에는 그대로 두세요.
> 실계좌(`REAL`)로 바꾸면 진짜 주문이 나갑니다.

## Firecrawl — 수업에서 같이 붙입니다

> 📄 교재 PDF **p.52** — Firecrawl 은 수업에서 같이 붙입니다

**기본 config 에 들어 있는 유일한 「키가 필요한 서버」입니다.** 수업에서 이것 하나로
「API 키를 받아 config 에 넣고 되살리는」 전 과정을 같이 해봅니다.

**폴더를 받을 필요가 없습니다.** `npx` 가 알아서 받아서 실행하므로, 가입하고 키만 넣으면 끝입니다.
그래서 배우기에 가장 안전한 예제입니다.

**제가 가장 많이 쓰는 MCP 중 하나입니다.** 이 교재를 만드는 동안에도 하루에 수십 번 썼습니다.

### 한마디로 — 웹페이지를 Claude 가 읽을 수 있는 글자로 바꿔줍니다

Claude 도 웹페이지를 열 수는 있습니다. 문제는 **요즘 사이트가 대부분 자바스크립트로 화면을 그린다**는 것입니다.
그래서 주소만 열면 **껍데기만 읽히고 정작 본문은 비어 있습니다.**

Firecrawl 은 서버에서 **진짜 브라우저를 대신 띄워** 화면을 끝까지 그린 뒤,
광고와 메뉴를 걷어내고 **본문만 깨끗한 글자로 뽑아** 건네줍니다.

### 어떤 자료가 이걸로 열리나

「보이는데 긁을 수 없는」 자료가 전부 여기 해당합니다.

| 분야 | 이런 것들 |
|---|---|
| **경매·공매** | 물건 상세 페이지, 공고문 전문 → 표로 정리 |
| **주식** | 공시 원문, 기사 본문 → 여러 건을 나란히 비교 |
| **부동산** | 단지 정보, 규제·제도 안내 페이지 |
| **일반 업무** | 복사가 막힌 페이지, 로그인·팝업에 걸려 못 읽던 자료 |

사람이 화면을 보며 일일이 옮겨 적던 일이 사라집니다.

### ★ Claude in Chrome 이 있어도 이건 따로 필요합니다

가장 많이 오해하는 지점입니다. **둘은 대체재가 아니라 서로 보완합니다.**

| | Claude in Chrome | Firecrawl |
|---|---|---|
| 어디서 도나 | **내 브라우저**를 눈앞에서 조작 | **서버**가 대신 접속해 읽어옴 |
| 한 번에 | 한 페이지씩, 내가 지켜보며 | 여러 페이지를 한 번에 |
| 로그인이 필요한 곳 | 내 세션을 그대로 씀 (**강점**) | 못 들어감 |
| 공개된 자료 대량 수집 | 탭을 하나씩 열어야 해 느림 | **이쪽이 압도적으로 빠름** |
| 사이트가 막아둔 경우 | **크롬 확장도 함께 막힙니다** | 우회해 읽히는 경우가 많음 |

**마지막 줄이 핵심입니다.** 크롬 확장을 붙여두어도 사이트가 자동화를 차단하면 그대로 막힙니다.
그때 Firecrawl 로는 읽히는 일이 자주 있습니다.

> 이 교재를 만들면서 실제로 겪은 일입니다.
> 어떤 서비스가 종료됐는지 확인해야 했는데 **크롬으로도 그 사이트가 열리지 않았습니다.**
> Firecrawl 로는 공식 도움말과 공지 게시판이 그대로 읽혀서,
> 「종료된 게 아니라 정기 점검이었다」는 사실을 확인할 수 있었습니다.
> **Firecrawl 이 없었으면 「확인 불가」로 끝났을 일입니다.**

로그인해서 봐야 하는 자료는 크롬 확장, 공개된 자료를 많이 읽어야 하면 Firecrawl —
**둘 다 붙여두시고 상황에 따라 쓰시는 것이 맞습니다.**

### 붙이는 법 ① — Claude 에게 물어보기 (빠릅니다)

먼저 `firecrawl.dev` 에 가입해 **왼쪽 메뉴 `API Keys`** 에서 키를 복사하세요.
키는 한 번만 온전히 보이는 경우가 많으니 **바로 메모장에 붙여두십시오.**

그다음 Claude 에게 이렇게 말하면 됩니다.

```
firecrawl MCP 를 붙이고 싶어.
config 에 넣을 블록을 만들어줘.
- 내 키: fc-○○○
```

받은 블록을 `설정 → 개발자 → 구성 편집` 으로 연 파일의 `"mcpServers": {` **바로 아래**에 붙여넣고,
**블록 끝 쉼표(`},`)** 를 확인한 뒤 저장하세요.

붙여넣은 화면을 캡처해 **「제대로 붙였는지 확인해줘」** 라고 물으면 쉼표·위치까지 봐줍니다.
캡처할 때는 **다른 MCP 의 `env` 블록을 접어** API 키가 함께 찍히지 않게 하십시오.

### 붙이는 법 ② — 직접 넣기 (구조가 보입니다)

블록 전문과 화면 단위 절차는 [Claude_MCP_연결_안내메시지.md](../Claude_MCP_연결_안내메시지.md) 에 있습니다.
결과는 ①과 같습니다. 구조를 알고 넘어가고 싶으시면 이쪽을 보세요.

### ★ 파이썬으로 만들지 마십시오

앞 절에서 「없는 MCP 는 Claude 에게 **파이썬으로** 만들어 달라」고 했는데,
**Firecrawl 은 공식 MCP 가 이미 있습니다.** `npx` 한 줄로 등록만 하면 끝입니다.
파이썬 파일을 만들려 하면 시간만 버립니다.

### 얼마나 드나 — 개인 업무는 무료로 충분합니다

| 항목 | 내용 |
|---|---|
| 무료 | **매달 1,000 크레딧 · 카드 등록 필요 없음** |
| 1페이지 읽기 | 1크레딧 |
| 검색 | 10건에 2크레딧 |
| 실패한 요청 | **과금하지 않습니다** |
| 크레딧 이월 | 안 됩니다. 매달 초기화됩니다 |

**하루 30페이지씩 읽어도 한 달 900건입니다.** 개인이 업무에 쓰는 수준이면 무료로 넉넉합니다.
더 필요하면 유료 요금제가 있지만, 처음부터 결제하실 필요는 없습니다.

> 사이트 전체를 통째로 훑는 `crawl` 은 한 번에 수백 크레딧이 나갑니다.
> **한 페이지씩 가져오는 `scrape`** 로 쓰시는 편이 안전합니다.
>
> > 출처: firecrawl.dev/pricing (2026-09-02 직접 조회)

> [!TIP]
> **GitHub 은 이 파일에 없습니다 — 커넥터로 붙이세요**
>
> GitHub 도 MCP 로 붙일 수 있지만, 설정 파일에 넣으려면 **Docker** 가 필요합니다.
> 다른 8개는 `npx`·`uv` 로 도는데 그것만 무겁습니다.
>
> **`설정` → `커넥터` 에서 붙이는 쪽이 훨씬 간단합니다.** 로그인만 하면 되고 설정 파일을 건드리지 않습니다.

## 당장 필요한 인증키는 하나입니다

> 📄 교재 PDF **p.50** — 인증키는 하나, 활용신청은 자료마다

| 키 | 발급처 | 넣는 곳 |
|---|---|---|
| **Firecrawl 키** | https://www.firecrawl.dev | **기본 config 에서 키가 필요한 유일한 곳.** 무료 · 카드 불필요 · `fc-` 로 시작 |
| 공공데이터포털 인증키 | https://www.data.go.kr | **수업 후에 쓰십니다.** 경매용 config 3곳 · datagokr 절 (전부 같은 값) |

**공공데이터포털은 계정당 키 하나입니다.** 서비스마다 키가 따로 나오지 않습니다.
다만 **서비스마다 「활용신청」은 따로 눌러야** 열립니다. 이걸 안 누르면 그 조회만 실패합니다.
키가 Encoding / Decoding 두 벌로 나오면 보통 **Decoding** 쪽을 씁니다.

**수업 전에 미리 발급받아 오시면 그날 시간을 벌 수 있습니다.**

> **나중에 트랙별로 늘어납니다.** 섹션 09 경매를 하시면 **브이월드 인증키**(https://www.vworld.kr) 와
> **법령센터 OC**(https://open.law.go.kr) 가 추가되고, 섹션 08 주식 실습을 하시면 **KIS · DART** 키가 필요합니다.
> 지금 다 받아둘 필요는 없습니다. 테마 레이더 실습은 키가 하나도 필요 없습니다.

## 커넥터와 config — 붙이는 길이 두 가지입니다

> 📄 교재 PDF **p.41 · p.46** — 붙이는 길은 두 가지 · MCP는 통로, API는 열쇠

같은 MCP라도 붙이는 길이 둘이고, 준비물이 다릅니다. 이 차이를 알아두시면 다음 단계가 수월합니다.

| 길 | 어떻게 | 준비물 | 이 문서에서는 |
|---|---|---|---|
| **커넥터** | 설정 → 커넥터 에서 목록에서 고르고 연결 | 대개 없음. 로그인만 | 섹션 08 (PlayMCP) |
| **설정 파일 직접 편집** | 개발자 → [구성 편집] 로 열어 붙여넣고 재시작 | Node.js · uv · 인증키 | 섹션 05 · 09 |

커넥터가 쉽고, 설정 파일 쪽이 훨씬 많은 것을 할 수 있습니다.
**섹션 08 실습은 커넥터 길로 갑니다. 섹션 09 경매 분석은 설정 파일 길로 갑니다.**

---


---

## 기본 config 에 들어 있는 MCP 여덟 개 — 무엇이고, 왜 이것을 골랐나

> 📄 교재 PDF **부록 D · p.112~120** 와 같은 내용입니다.
> 설정 파일에 무엇을 넣었는지 궁금하셨던 분을 위한 문서입니다.

`중요_claude_desktop_config_기본.json` 을 붙여넣으면 서버 **여덟 개**가 등록됩니다.
**일곱 개는 키가 없어도 그냥 켜지고, `firecrawl` 하나만 키가 필요합니다.**

---

## 한눈에 보기

| 이름 | 무엇을 해주나 | 준비물 |
|---|---|---|
| `memory` | 대화가 바뀌어도 기억합니다 | 없음 |
| `youtube-transcript` | 유튜브 자막을 가져옵니다 | 없음 |
| `context7` | 프로그램 공식 문서를 최신으로 봅니다 | 없음 |
| `sequential-thinking` | 어려운 문제를 단계로 쪼개 풉니다 | 없음 |
| `playwright` | 브라우저를 대신 눌러 줍니다 | 없음 |
| `hwp-read` | 한글 문서를 읽습니다 | 없음 |
| `word` | 워드 문서를 읽고 씁니다 | 없음 |
| **`firecrawl`** | 웹페이지를 글자로 뽑아 줍니다 | **★ API 키 1개** |

> [!NOTE]
> **키가 없는 상태로 붙여넣으면 `firecrawl` 하나만 붉게 표시됩니다. 고장이 아닙니다.**
> 나머지 일곱 개는 정상으로 켜집니다. 「열쇠가 없어 문을 못 열었다」는 뜻이지
> 「문이 부서졌다」는 뜻이 아닙니다.

---

## 왜 하필 이 여덟 개인가

스물여섯 개를 붙여 보고 남긴 것이 이것입니다. 고른 기준은 셋입니다.

1. **키가 없어도 켜질 것** — 가입하고 키 받다가 수업이 끝납니다
2. **폴더를 안 받아도 될 것** — 프로그램을 따로 깔아야 하는 것은 뺐습니다
3. **오늘 당장 쓸 데가 있을 것** — 안 쓰는 것을 붙여두면 오류만 늘어납니다

> [!IMPORTANT]
> **많이 붙일수록 좋은 것이 아닙니다.**
> 안 쓰는 서버는 켜질 때마다 실패하고, 그때마다 「뭐가 잘못됐지」 하고 시간을 씁니다.
> 필요한 것만 붙이십시오.

**공공데이터포털(`datagokr`) 은 뺐습니다.** 소스 폴더를 받아 따로 설치해야 해서
수업 중에 스무 명이 동시에 하기 어렵습니다. 자료는 그대로 다 있으니
경매 실습을 하실 분은 이 문서 앞쪽 datagokr 절 를 보고 수업이 끝난 뒤에 하십시오.

---

## ① `memory` — 기억합니다

Claude 는 **새 대화를 열면 앞의 이야기를 모두 잊습니다.** 매번 다시 설명해야 합니다.

`memory` 를 붙이면 알아둬야 할 것을 적어두고 다음 대화에서 꺼내 씁니다.

**이렇게 말하면 됩니다**

```
기억해줘. 내 관심 종목은 ○○, ○○ 두 개야.
```

```
기억해줘. 나는 오산·평택 쪽 경매 물건만 본다.
```

```
내가 전에 기억해달라고 한 것들 보여줘
```

> [!TIP]
> **지난주 배포본에 있던 `youtube` 를 이것으로 바꿨습니다 (2026-09-03).**
> 그 서버는 `yt-dlp` 라는 외부 프로그램이 따로 깔려 있어야 동작하는데,
> 「키 없이 바로 되는 것」이라고 안내한 것이 사실과 달랐습니다.
> **유튜브 자막·요약은 그대로 됩니다** — 아래 `youtube-transcript` 가 따로 있습니다.

---

## ② `youtube-transcript` — 영상을 읽습니다

유튜브 주소를 주면 **자막을 가져옵니다.** 한 시간짜리 영상을 다 보지 않고
요약만 받아볼 수 있습니다.

**이렇게 말하면 됩니다**

```
이 영상 요약해줘
https://www.youtube.com/watch?v=○○○○
```

```
이 영상에서 ○○에 대해 뭐라고 하는지만 찾아줘
```

> [!WARNING]
> **자막이 없는 영상은 안 됩니다.** 자동 생성 자막이라도 있어야 합니다.

---

## ③ `context7` — 옛날 답을 막습니다

Claude 는 **배운 시점까지의 지식만** 갖고 있습니다.
프로그램은 계속 바뀌는데 예전 방식으로 답하는 일이 생깁니다.

`context7` 은 그 프로그램의 **공식 설명서를 지금 시점으로 가져와서** 답합니다.

**이렇게 말하면 됩니다**

```
엑셀 XLOOKUP 함수 쓰는 법 알려줘. 최신 문서 확인해서.
```

```
파이썬 pandas 로 엑셀 여러 개 합치는 법 알려줘
```

엑셀 수식, 파이썬, 웹 만들기처럼 **「방법」을 묻는 일**에 특히 도움이 됩니다.

---

## ④ `sequential-thinking` — 쪼개서 풉니다

어려운 문제를 한 번에 답하려다 **중간을 건너뛰는 일**이 있습니다.

이것을 붙이면 문제를 여러 단계로 쪼개고, 앞 단계 결론을 확인하면서 넘어갑니다.

**이렇게 말하면 됩니다**

```
차근차근 단계별로 생각해서 풀어줘.
```

계산이 여러 번 겹치거나 조건이 복잡할 때 답이 눈에 띄게 좋아집니다.
**눈에 안 보이는 서버**라 처음에는 있는지도 모르실 수 있습니다.

---

## ⑤ `playwright` — 대신 눌러 줍니다

Claude 가 브라우저를 직접 열어서 **주소를 넣고, 누르고, 입력하고, 화면을 캡처**합니다.

사람이 로그인해야 보이는 화면, 여러 번 눌러야 나오는 자료를 대신 가져올 때 씁니다.

**이렇게 말하면 됩니다**

```
이 사이트 열어서 화면 캡처해줘
https://○○○
```

```
이 페이지에서 표 부분만 읽어서 엑셀로 정리해줘
```

> [!WARNING]
> **처음 쓸 때 브라우저를 한 번 내려받습니다.** 몇 분 걸립니다.
> 그동안 아무 반응이 없어 보여도 기다리십시오.

---

## ⑥ `hwp-read` — 한글 문서를 읽습니다

관공서 서류는 아직 **한글(hwp)** 이 많습니다. 그냥 올리면 Claude 가 못 읽는 경우가 있습니다.

이것을 붙이면 한글 문서의 **글자와 표를 그대로 읽어 옵니다.**

**이렇게 말하면 됩니다**

```
이 한글 파일 읽고 요약해줘
C:\경매\공고문.hwp
```

```
이 hwp 안에 있는 표를 엑셀로 옮겨줘
```

공고문, 계약서, 신청서를 다룰 때 한 번 붙여두면 계속 씁니다.

---

## ⑦ `word` — 워드를 읽고 씁니다

읽는 데서 그치지 않고 **직접 만들고 고칩니다.**
표를 넣고, 서식을 잡고, 쪽번호를 붙이고, PDF 로 바꾸는 것까지 합니다.

**이렇게 말하면 됩니다**

```
이 내용으로 워드 보고서 만들어줘. 표 넣고 쪽번호도 붙여줘.
```

```
이 워드 파일에서 「○○」를 「△△」로 전부 바꿔줘
```

보고서나 공문을 자주 쓰시면 이것 하나로 시간이 크게 줄어듭니다.

---

## ⑧ `firecrawl` — 웹을 글자로 뽑습니다 ★ 키가 필요합니다

요즘 사이트는 그냥 읽으면 **껍데기만** 들어옵니다.
화면을 자바스크립트로 그리기 때문입니다.

`firecrawl` 은 **본문만 글자로 뽑아** 줍니다.

**이렇게 말하면 됩니다**

```
이 페이지 내용 읽어서 정리해줘
https://○○○
```

```
이 사이트에서 ○○ 관련 페이지들 찾아서 표로 만들어줘
```

### 키 넣는 법

`firecrawl.dev` 에서 무료 가입하면 `fc-` 로 시작하는 키를 줍니다.
**카드는 필요 없습니다.**

설정 파일에서 이 자리를 찾아 꺾쇠 부분을 지우고 받은 키를 넣으십시오.

```json
"firecrawl": {
  "command": "npx",
  "args": ["-y", "firecrawl-mcp"],
  "env": {
    "FIRECRAWL_API_KEY": "fc-여기에받은키",
    ...
  }
}
```

> [!IMPORTANT]
> **따옴표는 지우지 마십시오.** 꺾쇠 안의 글자만 지우고 키를 넣습니다.
> 넣은 뒤에는 **반드시 Claude 를 완전히 껐다 켜야** 합니다.

**요금** — 매달 1,000 크레딧 무료 · 1페이지 읽기가 1크레딧 · 검색은 10건에 2크레딧 ·
**실패한 요청은 과금되지 않습니다** (firecrawl.dev/pricing, 2026-09-05 조회)

---

## 안 켜질 때는 이 순서로

대부분 **두 번째**에서 해결됩니다.

| 순서 | 확인할 것 |
|---|---|
| 1 | 설정 파일을 **저장**했는가 (메모장에서 `Ctrl+S`) |
| 2 | Claude 를 **완전히** 껐다 켰는가 |
| 3 | 쉼표와 괄호가 맞는가 |
| 4 | Node.js 와 uv 가 깔려 있는가 |

> [!WARNING]
> **창의 `X` 는 끄는 것이 아닙니다.**
> 작업 표시줄 오른쪽 트레이에서 Claude 아이콘을 찾아 **종료**해야 진짜로 꺼집니다.
> 여기서 제일 많이 막힙니다.

> [!IMPORTANT]
> **빨간불과 빈 답은 원인이 다릅니다.**
>
> - **빨간불** = 서버가 **아예 안 떴다** (설정 오류 · 프로그램 없음)
> - **빈 답** = **떴는데 자료를 못 받았다** (키 없음 · 권한 없음)
>
> 자세한 것은 [MCP 는 어떻게 도는가](../MCP_동작원리.md) 를 보십시오.

**그래도 안 되면** 화면을 그대로 캡처해서 Claude 에게 보여 주십시오.
「이렇게 넣었는데 안 켜집니다」 하고 붙이시면 어디가 틀렸는지 짚어 줍니다.
**혼자 붙들고 계시지 마십시오.** 설정 파일 오류는 눈으로 찾기 어렵습니다.

---

## 여기 없는 것들 — 나중에 붙이실 것

기본 config 에는 안 넣었지만 쓸모가 큰 것들입니다. **필요해지면 그때 붙이십시오.**

| 무엇 | 언제 필요한가 | 어디를 보나 |
|---|---|---|
| **datagokr** | 경매·부동산 실거래가, 공매(온비드) 자료 | 이 문서 앞쪽 datagokr 절 · 0~6단계 |
| **Claude in Chrome** | 로그인해야 보이는 화면을 다루고 싶을 때 | [Claude_in_Chrome_설치안내.md](../Claude_in_Chrome_설치안내.md) |
| **Excel MCP** | 엑셀을 Claude 가 직접 조종하게 하고 싶을 때 | [Excel_MCP_설치안내.md](../Excel_MCP_설치안내.md) |
| **PlayMCP (카카오)** | 카카오맵·카카오톡 도구를 쓰고 싶을 때 | [Claude_PlayMCP_연결안내.md](../Claude_PlayMCP_연결안내.md) |
| **한국투자증권** | 주식 시세·잔고를 직접 조회하고 싶을 때 | 이 문서 앞쪽 의 `kis` 절 |

> [!TIP]
> **공식 MCP 가 아예 없는 서비스라면 Claude 에게 만들어 달라고 하시면 됩니다.**
> `datagokr` 이 바로 그렇게 만든 것입니다.
> 만드는 법은 이 문서 앞쪽 의 datagokr 절에 0~6단계로 적어두었습니다.
>
> 그 절 맨 앞의 **「이것 하나만 알면 MCP 전체가 이해됩니다 — 경로와 파일」** 을
> 꼭 한 번 읽어보십시오. **설정에 적는 줄은 명령이 아니라 주소**이고,
> 그 주소 자리에 진짜 파일이 있어야 켜진다는 이야기입니다.

---

*이 문서는 교재 부록 D(p.112~120) 와 같은 내용입니다. 화면 확인 2026-09-05*

---

← [04 회원가입과 커넥터](04_회원가입과커넥터.md) · [목차](../README.md) · [06 스킬](06_스킬.md) →
