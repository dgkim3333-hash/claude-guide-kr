# Excel MCP 설치 안내

Claude 가 **열려 있는 엑셀을 직접 조종**하게 해주는 확장 프로그램입니다.
파일을 새로 쓰는 게 아니라 실제 엑셀을 움직이므로, 표·슬라이서·피벗이 그대로 살아 있는 채로 고쳐집니다.

> 여기서 많이 막힙니다. 순서대로 천천히 따라오세요.

---

## 먼저 확인할 것

| 조건 | 내용 |
|---|---|
| 운영체제 | **Windows 전용**입니다. 맥은 안 됩니다 |
| 엑셀 | **Microsoft Excel 2016 이상**이 깔려 있어야 합니다. **한셀만으로는 안 됩니다** |
| Claude | 데스크탑 앱 (웹은 안 됩니다) |

---

## 1. 파일 받기

받는 곳은 GitHub 의 `sbroenne/mcp-server-excel` 저장소 릴리스 페이지입니다.

```
https://github.com/sbroenne/mcp-server-excel/releases/latest
```

- 릴리스 페이지에 파일이 여러 개 있습니다. **받을 것은 `excel-mcp-….mcpb` 하나뿐**입니다.
  `.vsix` 나 `.zip` 은 다른 용도라 받아도 소용없습니다.
- 버전 번호는 계속 바뀝니다. **「최신 릴리스(latest)」에서 받으면 됩니다.**
- 파일이 **약 67MB** 로 큽니다. 회사망에서는 막히거나 오래 걸릴 수 있습니다.
- 파일 이름에 오른쪽 클릭해서 「다른 이름으로 링크 저장」을 하면 **웹페이지가 저장됩니다.**
  파일 이름을 **왼쪽 클릭**해 그대로 내려받으세요.

**이렇게 되면 성공** — 다운로드 폴더에 `.mcpb` 로 끝나는 파일 하나가 생깁니다.

## 2. 설치하기

1. Claude 데스크탑 앱을 엽니다
2. `설정 > 확장 프로그램` 으로 갑니다
3. 받은 `.mcpb` 파일을 그 화면에 **끌어다 놓습니다**
4. 설치 확인이 뜨면 진행합니다

**이렇게 되면 성공** — 확장 프로그램 목록에 Excel 항목이 생기고 토글이 켜집니다.

> 화면이 두 개라 헷갈립니다 — `설정 > 확장 프로그램`(설치·토글)과
> `데스크톱 앱 > 확장 프로그램`(세부 설정)은 다른 화면입니다. 설치는 앞쪽에서 합니다.

## 3. 확인하기

1. **열려 있는 엑셀 파일을 전부 닫습니다** (이게 제일 중요합니다)
2. **새 대화**를 엽니다 — 기존 대화에는 적용되지 않습니다
3. "엑셀을 새로 하나 열어서 A1 에 '테스트'라고 써줘" 라고 시켜봅니다

**이렇게 되면 성공** — 엑셀이 실제로 뜨고 글자가 입력됩니다.

---

## 안 될 때

| 증상 | 대처 |
|---|---|
| 「서버에 연결할 수 없습니다」 | **엑셀 파일이 열려 있으면 안 됩니다.** 전부 닫고, 토글을 껐다 켠 뒤, 새 대화에서 다시 시도 |
| `Private dir leaf redirects` 오류 | `%APPDATA%\Claude\Claude Extensions Settings` 폴더를 직접 만들고 재시도 |
| 설치했는데 도구가 안 보임 | **새 대화**에서 확인했는지 봅니다. 기존 대화에는 안 붙습니다 |
| 받은 파일이 열리지 않음 | `.mcpb` 는 직접 여는 파일이 아닙니다. 설정 화면에 끌어다 놓는 용도입니다 |

> 확장 프로그램은 `claude_desktop_config.json` 에 기록되지 않습니다.
> 설정 파일을 아무리 뒤져도 안 나오는 게 정상입니다.

---

출처: github.com/sbroenne/mcp-server-excel 릴리스 페이지 (2026-08-28 직접 조회)
