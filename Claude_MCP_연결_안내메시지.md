# 마지막 한 걸음이 남았습니다 — MCP 연결

여기까지 오시느라 정말 고생 많으셨습니다.
지침을 넣고, 스킬을 켜고, 커넥터를 붙이는 것까지 —
처음 해보는 설정이 줄줄이 이어져서 지치셨을 겁니다.

**그런데 딱 한 걸음이 남았습니다. 그리고 그 한 걸음이 가장 큰 걸음입니다.**

---

## MCP를 연결하지 않으면, Claude의 절반도 쓰지 않는 것입니다

지금까지의 Claude는 **여러분이 올려준 것만** 볼 수 있습니다.
자료를 찾고, 내려받고, 올리는 일은 전부 사람 몫이었습니다.

MCP를 연결하면 달라집니다.

| | MCP 없이 | MCP를 연결하면 |
|---|---|---|
| 실거래가 | 직접 찾아서 정리해 올림 | **"이 단지 최근 6개월 실거래가 뽑아줘"** |
| 기업 재무 | 공시 사이트에서 받아 올림 | **"이 회사 최근 3년 재무제표 비교해줘"** |
| 법령·판례 | 검색해서 복사해 옴 | **"이 조항 원문 그대로 가져와줘"** |

차이는 편의가 아닙니다. **자료를 직접 가져오느냐 아니냐**의 차이입니다.
여기까지 하신 설정은 전부 이 한 걸음을 위한 준비였습니다.

**잠시 쉬셨다가, 꼭 연결하십시오.**

---

## 좋은 소식 — 대부분은 이미 만들어 두었습니다

MCP는 원래 설정 파일을 직접 써야 해서 손이 많이 갑니다.
그래서 **API 키가 필요 없는 것들은 미리 다 만들어 두었습니다.**

GitHub에 올려둔 파일입니다.

```
중요_claude_desktop_config_기본.json
```

이 파일의 내용을 여러분의 `claude_desktop_config.json` 에 **복사해 붙여넣으면 그대로 동작합니다.**

### 붙여넣는 방법 — 천천히 따라오시면 됩니다

이 부분이 가장 낯설 겁니다. **어려운 게 아니라 낯선 것**입니다. 순서대로만 하시면 됩니다.

**1 · GitHub에서 파일을 내려받아 엽니다**

1. GitHub에서 `중요_claude_desktop_config_기본.json` 을 엽니다
2. 코드 화면 오른쪽 위의 **다운로드 화살표(⬇)** 를 누릅니다
3. 브라우저 오른쪽 위에 다운로드 알림이 뜨면 **열기** 를 누릅니다
4. **VS Code로 열립니다**

**2 · 내 설정 파일을 엽니다**

Claude 앱에서 이렇게 갑니다.

```
설정 → 개발자 → 구성 편집
```

**구성 편집** 을 누르면 탐색기가 열리면서 `claude_desktop_config.json` 이 선택된 상태로 나타납니다.
그 파일을 **더블클릭**하면 VS Code로 열립니다.

> **앞으로 설정 파일을 열 때는 항상 이 방법을 쓰십시오.**
> 폴더 경로를 외울 필요가 없고, 설치 방식에 따라 위치가 달라지는 문제도 없습니다.

**3 · 두 파일을 나란히 놓고 옮깁니다**

지금 VS Code에 파일이 **두 개** 열려 있습니다. 위쪽 탭으로 오갈 수 있습니다.

| 순서 | 어느 파일에서 | 누를 것 |
|---|---|---|
| 1 | `중요_claude_desktop_config_기본.json` | `Ctrl` + `A` → `Ctrl` + `C` |
| 2 | `claude_desktop_config.json` 탭으로 이동 | |
| 3 | `claude_desktop_config.json` | `Ctrl` + `A` → `Ctrl` + `V` → `Ctrl` + `S` |

**이게 전부입니다.**

기존 내용은 지워집니다. 처음 설정하시는 분은 어차피 비어 있으니 그대로 하시면 됩니다.

**4 · 빨간 줄이 없는지 확인**

이 파일은 쉼표나 괄호가 **하나만 어긋나도 전체가 읽히지 않습니다.**
VS Code는 문제가 있으면 그 자리에 **빨간 물결선**을 그어줍니다.

- 빨간 줄이 **없으면** 정상입니다
- 빨간 줄이 **있으면** 그 줄을 확인하십시오. 대부분 쉼표가 빠졌거나 하나 더 있는 경우입니다

**5 · datago 자리에 내 키 넣기**

`Ctrl` + `F` 를 눌러 `datago` 를 검색합니다.
그 근처에 키를 넣는 자리가 있습니다. 따옴표 **안쪽에** 본인 키를 붙여넣으십시오.

```
"API_KEY": "여기에 본인 키"
```

> 따옴표를 지우지 마십시오. 따옴표 사이의 내용만 바꾸는 것입니다.

키는 아래 「datago」 절차대로 받으시면 됩니다. **키를 아직 안 받으셨다면 이 단계는 나중에 하셔도 됩니다.**
나머지 MCP는 키 없이도 먼저 동작합니다.

**6 · 저장하고 Claude 재시작**

`Ctrl` + `S` 로 저장한 뒤, 뒤에 나오는 **「Claude 완전 종료」** 절차를 따르십시오.

---

## 딱 하나, 직접 하셔야 하는 것 — datago

`datago`(공공데이터포털)도 위 파일에 **이미 포함시켜 두었습니다.**
다만 이것만은 **API 키를 본인이 직접 받아야** 합니다. 키는 개인에게 발급되는 것이라 대신 넣어드릴 수 없습니다.

**왜 하필 datago부터인가**

여기에 실무 자료가 가장 많이 들어 있습니다.
부동산 실거래가, 공매 물건과 낙찰 결과, 건축물대장, 사업자등록 상태, 기업 재무정보 —
블로그에서 본 추정치가 아니라 **기관이 직접 올린 원본**입니다.

그리고 하나 더 있습니다.

> **datago 연결에 성공하면, 나머지 MCP 연결은 훨씬 쉽습니다.**
> 절차가 거의 같기 때문입니다. 첫 번째가 어렵지 두 번째부터는 금방 하십니다.

### 받는 순서 — 처음 한 번만 하면 됩니다

**1단계 · 회원가입**

```
https://www.data.go.kr
```

일반 회원으로 가입합니다. **무료**입니다.

**2단계 · 쓸 자료를 찾아 「활용신청」**

검색 결과에 **파일데이터**와 **오픈 API** 두 가지가 섞여 나옵니다.
엑셀을 내려받는 것이 파일데이터, 프로그램이 불러 쓰는 것이 오픈 API입니다.
**반드시 「오픈 API」 쪽을 고르십시오.**

자료를 열어 **활용신청** 버튼을 누르고, 활용목적을 한 줄 적고, **상세기능은 전부 체크**한 뒤 동의하면 끝입니다.

> **여기가 가장 많이 오해하는 지점입니다.**
> **오픈 API 키 하나로 모든 자료가 열리는 것이 아닙니다.**
> 자료 하나하나마다 활용신청을 따로 해야 합니다.
>
> 실거래가만 해도 「아파트 매매」와 「아파트 전월세」가 서로 다른 자료입니다.
> 쓸 자료가 다섯 개면 신청도 다섯 번입니다. 한 번에 몰아서 해두시는 편이 낫습니다.

**3단계 · 승인 확인**

```
마이페이지 → Open API → 활용신청 현황
```

**활용 중 · 승인** 칸에 숫자가 올라가면 쓸 수 있는 상태입니다.
대부분 신청 즉시 승인되고, 일부만 하루 이틀 걸립니다.

**4단계 · 오픈 API 키 확인**

```
마이페이지 → Open API → 인증키 발급현황
```

> 사이트에서는 **「인증키」** 라고 표시되어 있습니다. **오픈 API 키와 같은 말**입니다.
> 화면에서 그 이름을 찾으셔야 하므로 기억해 두십시오.

여기서 두 가지만 알아두시면 됩니다.

- **키는 계정당 하나입니다.** 자료를 몇 개 신청했든 키는 같습니다. 자료마다 키를 따로 찾을 필요가 없습니다
- **`Encoding` 과 `Decoding` 두 줄로 보입니다.** 같은 키를 다르게 표기한 것입니다.
  **설정 파일에는 `Decoding` 쪽을 넣으십시오**

**5단계 · 설정 파일에 넣고 Claude 재시작**

Claude에서 **설정 → 개발자 → 구성 편집** 을 눌러 `claude_desktop_config.json` 을 엽니다.
`Ctrl` + `F` 로 `datago` 를 찾아, 키 자리에 붙여넣고 저장합니다.

그다음 **Claude를 완전히 종료**해야 합니다.

> **창을 닫는 것만으로는 종료되지 않습니다.**
> 오른쪽 위 `X` 를 눌러도 Claude는 뒤에서 계속 돌아갑니다.
> 이 상태로 다시 열면 **바뀐 설정이 반영되지 않습니다.**

**완전히 종료하는 방법**

1. 화면 오른쪽 아래 작업표시줄에서 **`∧`**(숨겨진 아이콘 표시)를 누릅니다
2. 아이콘 중에서 **Claude 아이콘**(검은 별 모양)을 찾습니다
3. 그 아이콘 위에서 **마우스 오른쪽 버튼**을 누릅니다
4. **종료**(Quit)를 선택합니다
5. 아이콘이 사라진 것을 확인한 뒤 Claude를 다시 실행합니다

**마지막으로, 반드시 새 대화에서 확인하십시오.**

> 대화가 시작되는 순간에 쓸 수 있는 도구가 정해집니다.
> 이미 열려 있던 대화에서는 재시작을 해도 잡히지 않습니다.

정리하면 **저장 → 트레이에서 종료 → 다시 실행 → 새 대화** 순서입니다.
이 네 가지 중 하나만 빠져도 "설정했는데 안 된다"가 됩니다.

**자세한 화면 설명은 별도 안내문에 있습니다** — 「공공데이터포털 오픈API 인증키 받는 방법」

---

## 그다음 — 나머지 키는 여기서 받습니다

한 번에 다 하실 필요 없습니다. **필요할 때 하나씩** 받으면 됩니다.

| 서비스 | 어디서 받나 | 무엇에 쓰나 |
|---|---|---|
| **국가법령정보** | `open.law.go.kr` | 법령·판례 원문. **이메일만으로 발급** |
| **DART** | `opendart.fss.or.kr` | 기업 공시와 재무제표 |
| **V-World** | `vworld.kr` | 토지이용계획·지적도 |
| **한국투자증권** | `apiportal.koreainvestment.com` | 주식 시세. 증권 계좌가 있어야 신청 |
| **Firecrawl** | `firecrawl.dev` | 봇 차단 사이트 자료 수집 |
| **옵시디언** | 플러그인 안에서 직접 발급 | 내 노트를 읽고 씀 |
| ~~네이버 검색~~ | `developers.naver.com` | **★ 현재 신규 신청 중단** |

가장 쉬운 것부터 하시려면 **국가법령정보**를 권합니다. 이메일만 넣으면 바로 나옵니다.

---

## 그런데 키만 받아서는 되지 않습니다

여기가 **아무도 알려주지 않으면 못 하는 부분**입니다. 꼭 읽어주십시오.

`datago` 는 **누군가 이미 만들어 둔 MCP**가 있어서 설정 파일에 적기만 하면 됐습니다.
하지만 위 표의 서비스들은 **그런 것이 없습니다.**

키를 받아도 붙일 곳이 없습니다. **MCP를 만들어야 합니다.**

### 하지만 여러분이 코딩하는 것이 아닙니다

**Claude에게 만들어 달라고 하면 됩니다.**

파이썬을 몰라도 됩니다. 여러분이 할 일은 세 가지를 알려주는 것뿐입니다.

| 알려줄 것 | 예 |
|---|---|
| **무엇을 하고 싶은지** | "복사가 막힌 웹페이지 내용을 글자로 읽어오고 싶다" |
| **API 문서 주소** | `https://docs.firecrawl.dev` |
| **내 키** | 발급받은 값 |

나머지는 Claude가 다 합니다.

### 실제로 해보기 — Firecrawl

복사가 막힌 웹페이지를 대신 읽어다 주는 서비스입니다.

**1 · 오픈 API 키 받기**

```
https://www.firecrawl.dev
```

Sign Up → 로그인 → 왼쪽 메뉴 **API Keys** → 키 복사

> 키는 화면에 한 번만 온전히 보이는 경우가 많습니다. 복사해서 안전한 곳에 먼저 붙여넣어 두십시오.

**2 · 설정 파일에 아래를 추가합니다**

**설정 → 개발자 → 구성 편집** 으로 `claude_desktop_config.json` 을 엽니다.

```json
"firecrawl": {
  "command": "npx",
  "args": [
    "-y",
    "firecrawl-mcp"
  ],
  "env": {
    "FIRECRAWL_API_KEY": "fc-여기에_본인_키",
    "FIRECRAWL_RETRY_MAX_ATTEMPTS": "5",
    "FIRECRAWL_RETRY_INITIAL_DELAY": "2000",
    "FIRECRAWL_RETRY_MAX_DELAY": "30000",
    "FIRECRAWL_RETRY_BACKOFF_FACTOR": "3",
    "FIRECRAWL_CREDIT_WARNING_THRESHOLD": "2000",
    "FIRECRAWL_CREDIT_CRITICAL_THRESHOLD": "500"
  }
},
```

**어디에 붙여넣나**

방금 **구성 편집**으로 연 여러분의 `claude_desktop_config.json` 입니다.
그 파일 맨 위가 이렇게 되어 있습니다.

```json
{
  "mcpServers": {
    "youtube": {
```

**`"mcpServers": {` 바로 아래**에 붙여넣습니다. 그러면 이렇게 됩니다.

```json
{
  "mcpServers": {
    "firecrawl": {          ← 여기부터
      "command": "npx",
      ...
      }
    },                      ← 여기까지가 붙여넣은 것
    "youtube": {            ← 원래 있던 것
```

**블록 끝의 쉼표(`},`)를 빠뜨리지 마십시오.** 뒤에 다른 항목이 이어지기 때문에 필요합니다.

그리고 `FIRECRAWL_API_KEY` 자리에 1단계에서 복사한 키를 넣습니다. **따옴표는 지우지 않습니다.**

**제대로 붙였는지 확인하는 방법**

저장한 뒤, **VS Code 화면을 캡처해서 Claude에게 보여주십시오.**

```
설정 파일에 firecrawl을 추가했어. 제대로 붙였는지 확인해줘.
```

캡처 이미지와 함께 이렇게 물으면 Claude가 봐줍니다.
쉼표가 빠졌는지, 위치가 맞는지, 괄호가 안 닫혔는지 **눈으로 확인해서 알려줍니다.**

> 혼자 들여다보며 고민하지 마십시오. **캡처해서 물어보는 것이 가장 빠릅니다.**
> 이 방법은 앞으로 설정 파일을 고칠 때마다 그대로 쓰시면 됩니다.

**캡처할 때 주의하십시오 — 키가 함께 찍힙니다**

설정 파일에는 여러 MCP의 API 키가 **가려지지 않은 글자 그대로** 들어 있습니다.
화면에 보이는 것은 캡처에도 그대로 남습니다.

캡처 전에 지금 확인하는 부분만 남기고 나머지는 가리십시오.

- VS Code에서 줄 번호 왼쪽의 **`∨`** 를 누르면 그 블록이 접힙니다. 다른 MCP의 `env` 블록을 접어두십시오
- 또는 확인이 필요한 부분만 **화면에 보이도록 스크롤**한 뒤 캡처하십시오

> 혹시 키가 찍힌 화면을 단체 대화방이나 발표 자료에 올리셨다면,
> **보안상 그 키는 새로 받으시기를 강력히 권합니다.** 발급은 몇 분이면 됩니다.

**3 · Claude 완전 종료 → 재실행 → 새 대화**

```
https://www.data.go.kr 페이지 읽어서 무슨 사이트인지 알려줘
```

내용이 나오면 성공입니다.

### 공식 MCP가 없는 서비스라면

위처럼 등록할 것이 없는 경우입니다. 그때는 Claude에게 만들어 달라고 하십시오.

```
○○ API로 MCP 서버를 만들어줘.

- API 문서: (문서 주소)
- 내 API 키: (본인 키)
- 하고 싶은 것: (한 줄로)

파이썬으로 만들어주고,
claude_desktop_config.json 에 넣을 부분도 함께 알려줘.
파일은 C:\mcp\ 폴더에 저장할 거야.
```

파이썬 파일 하나와 설정에 넣을 조각을 만들어 줍니다.
파일은 VS Code에 붙여넣어 저장하고, 조각은 설정 파일에 추가한 뒤 재시작하면 됩니다.

### 안 되면 오류를 그대로 보여주십시오

처음에 한 번에 되는 경우는 드뭅니다. **정상입니다.**

빨간 글씨가 뜨면 **그 내용을 그대로 복사해서 Claude에게 붙여넣으십시오.**

```
이렇게 나왔어. 고쳐줘.

(오류 내용 붙여넣기)
```

Claude가 원인을 찾아 고친 코드를 다시 줍니다. **이 과정을 두세 번 반복하면 대부분 됩니다.**

> 이것이 이 방식의 진짜 장점입니다.
> **혼자 해결하는 것이 아니라, 오류를 그대로 전달하면 되는 것**입니다.

### 하나만 성공하면 나머지는 같습니다

DART도, V-World도, 한국투자증권도, 국가법령정보도 **절차가 똑같습니다.**
바뀌는 것은 **API 문서 주소와 키뿐**입니다.

첫 번째만 넘으십시오. 두 번째부터는 30분이면 됩니다.

가장 쉬운 것으로 연습하고 싶으시면 **국가법령정보**(`open.law.go.kr`)를 권합니다.
이메일 주소만 넣으면 키가 나와서, 키 받는 단계에서 막힐 일이 없습니다.

> **만들기 전에 한 번 검색해 보십시오.**
> 서비스에 따라 **이미 공식 MCP가 나와 있는 경우**가 있습니다.
> Claude에게 "○○의 공식 MCP가 있는지 찾아줘" 라고 물어보면 됩니다.
> 있으면 직접 만들 필요 없이 주소만 등록하면 끝입니다.

---

## 막히면 연락 주십시오

설정 파일을 만지다 보면 반드시 한 번은 막힙니다. **정상입니다.**
혼자 붙들고 계시지 말고 언제든 연락 주십시오.

- 어느 단계에서 막혔는지
- 화면에 뜬 메시지

이 두 가지만 알려주시면 대부분 금방 풀립니다.

여기까지 오신 것만으로도 이미 대부분의 사람보다 앞서 계십니다.
**마지막 한 걸음만 더 하십시오.**
